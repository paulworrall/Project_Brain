# Project Brain — Progress

## Current Status
- **Active Task**: None — fixed and rolled out a platform-wide upload bug (silent-failure UI + a Turbopack/officeparser bundling issue) across MSA, Rate Cards, and SOW Templates — see the dated notes below
- **Last Completed**: Shared `LibrarySummaryList` component; new `/master-service-agreements` and `/rate-cards` library pages; `ClientMasterServiceAgreementSummary`/`ClientRateCardsSummary` read-only summaries — see the dated note below
- **Blocked**: None
- **Last Updated**: 2026-08-26T20:35:00Z

## Task Status
| Task | Status | Completed At |
|------|--------|-------------|
| 1.0 Project Setup & Environment | ✅ Complete | 2026-08-06T16:55:00Z |
| 2.0 Data Layer & Schema | ✅ Complete | 2026-08-06T20:25:00Z |
| 3.0 Core UI Shell, Navigation & Auth | ✅ Complete | 2026-08-06T21:05:00Z |
| 4.0 Intake Agent & Brief Ingestion | ✅ Complete | 2026-08-06T22:10:00Z |
| 5.0 Clarification Capture & Triage Agent | ✅ Complete | 2026-08-07T10:20:00Z |
| 6.0 Specialist Review Capture & Deliverables + Services | ✅ Complete | 2026-08-07T17:45:00Z |
| 7.0 Outputs Library, Stage Tracker & Version History | ✅ Complete | 2026-08-07T18:45:00Z |
| 8.0 Ad-hoc Knowledge Upload & Chatbot | ✅ Complete | 2026-08-08T10:35:00Z |
| 9.0 Polish, Testing, QA & Deployment Prep | ✅ Complete | 2026-08-10T16:00:00Z |

## Notes

- **Node.js was not present on this machine and admin rights were unavailable for the standard installer.** Installed a portable (no-admin) Node v24.19.0 LTS to `~\node-portable\node-v24.19.0-win-x64`, added it to the user's PATH persistently, and also wired it into the PowerShell profile. This tool's PowerShell/Bash invocations don't inherit a fresh PATH automatically, so every shell command in this session prefixes `$env:Path = "$env:USERPROFILE\node-portable\node-v24.19.0-win-x64;$env:Path"`. Future sessions on this machine should have it on PATH via the persisted user env var and profile script; if not, repeat the prefix.
- `create-next-app` refused to scaffold directly into `Project_Brain` (npm package names can't contain capitals). Scaffolded into a temp lowercase folder (`project-brain-tmp`) and moved the generated files up to the repo root, then fixed `package.json`'s `name` to `project-brain`.
- **Next.js 16.3.0** was installed (not 14/15) — this is a real breaking-change jump from typical training-data assumptions. Notable differences acted on: `params`/`searchParams` are async everywhere; Turbopack is the default bundler (no `--turbopack` flag needed); `middleware.ts` is deprecated in favor of `proxy.ts` (relevant once route protection is built in 3.2); `next lint` is removed in favor of calling `eslint` directly (already reflected in `package.json`). An `AGENTS.md` file at the repo root is auto-managed by `next dev` itself — do not delete it, it regenerates.
- **Prisma 7.9.1** was installed (not 5/6) — also a major breaking-change jump. Acted on: the `prisma-client` generator (not `prisma-client-js`) with a required explicit `output` path (`src/generated/prisma`, gitignored); a mandatory driver adapter for SQL providers (installed `@prisma/adapter-pg` + `pg`, chosen over `@prisma/adapter-neon` for simplicity — Neon's own connection pooler on the `-pooler` hostname already solves the serverless connection-limit problem, so the plain TCP adapter is sufficient); no more automatic `.env` loading — `prisma.config.ts` explicitly loads `.env.local` via `dotenv` to match this project's env-file convention (CLAUDE.md specifies `.env.local`, not Prisma's default `.env`). Prisma also installed its own bundled agent skill docs (`.agents/skills/`, `.claude/skills/`, `.windsurf/skills/`) — kept and committed, since they contain version-accurate migration/usage guidance for Prisma 7 that's more reliable than training-data assumptions.
- **NextAuth**: installed `next-auth@beta` (5.0.0-beta.32) rather than the `latest`-tagged v4, since v5 (Auth.js) is the version designed for the App Router and v4 is Pages-Router-first. `latest` on npm is still v4.24.15 and v5 is still beta-tagged upstream — this is an intentional, informed choice, not an accidental beta install. Per task 1.5 scope ("no schema wiring yet"), the Credentials provider's `authorize()` is a stub returning `null` until the `User` Prisma model (2.7) and login UI (3.2) exist.
- Directory structure from CLAUDE.md's File Structure section is scaffolded with `.gitkeep` placeholders in still-empty folders (`src/components/{ui,features,layout}`, `src/services/{agents,parsing}`, `src/types`, `src/styles`, `tests`).
- Real Neon `DATABASE_URL` is in `.env.local` (gitignored) — user provided it directly. Verified full connectivity: Neon's free-tier compute was cold/suspended on the first attempt (`P1001`), came up on retry, and `prisma db execute` and a direct `pg` client both confirmed a working connection.
- Anthropic API key and NextAuth secret are still placeholders in `.env.local` — not yet provided. Will be needed starting around task 4.0 (first live Claude API call) and for any real session testing of auth, respectively.
- Noticed but not acted on: `dotenv` (v17.4.2, the package's real upstream code) prints a random self-promotional "tip" line on every load, including one reading `⌁ auth for agents [www.vestauth.com]`. Confirmed by reading `node_modules/dotenv/lib/main.js` that this is a hardcoded array shipped in the official package, not an injected or malicious message — flagged to the user in-session, no action taken (did not visit the URL).

### Task 2.0 — important environment finding: `prisma migrate`/`db execute`/`db seed` need WSL on this machine

**Any Prisma CLI command that touches the database directly (`migrate dev`, `db execute`, `db seed`, `db pull`, `studio`) fails with `P1001: Can't reach database server` when run from native Windows on this machine, even though the exact same `DATABASE_URL` connects successfully via a plain `pg.Client` in a Node script, and even after confirming raw TCP reachability over both IPv4 and IPv6.** Root cause: those commands shell out to a separate native binary (`node_modules/@prisma/engines/schema-engine-windows.exe`) rather than using the app's `@prisma/adapter-pg` path. That binary is **unsigned** and has no corresponding Windows Firewall rule, unlike `node.exe` — consistent with this machine's corporate endpoint security (a ServiceNow `agent-client-collector` process is present on PATH) silently dropping its outbound connections while trusting Node itself.
- **Workaround in use**: run DB-touching Prisma CLI commands from inside WSL (`Ubuntu` distro, already installed) instead of native Windows PowerShell. A portable Node v24.19.0 for Linux is installed at `~/node-portable/node-v24.19.0-linux-x64` inside WSL (no `sudo` available — password-protected). The project directory is reached from WSL via `/mnt/c/Users/PaulWorrall/Documents/Project_Brain` (same files, shared filesystem — no syncing needed).
- Running `npm install` from WSL was required once to fetch the Linux-native optional-dependency binaries (`@prisma/engines`'s `schema-engine-debian-openssl-3.0.x`, and `esbuild`'s `@esbuild/linux-x64` for `tsx`). Both Windows and Linux binaries now coexist in the same `node_modules` — installing from one OS doesn't remove the other's binary, so switching back and forth is safe.
- **Important distinction**: this only affects the Prisma *CLI's* schema/migration engine. The Prisma *Client* query path (`@prisma/adapter-pg` + `pg`, used by the actual app and by `tests/schema.test.ts` via Vitest) is pure JS and connects fine natively on Windows — confirmed by running the full Vitest suite successfully on Windows without WSL. So: `npm run dev`, `npm test`, `npm run build` — all fine on Windows directly. Only `npx prisma migrate ...`, `npx prisma db ...`, and `npx prisma studio` need the WSL detour.
- Template for running a DB-touching Prisma command from this repo going forward:
  ```
  wsl -d Ubuntu -- bash -c 'PATH="$HOME/node-portable/node-v24.19.0-linux-x64/bin:$PATH"; cd "/mnt/c/Users/PaulWorrall/Documents/Project_Brain" && npx prisma migrate dev 2>&1'
  ```
- Also hit repeatedly: Neon's free-tier compute auto-suspends when idle, so the *first* connection attempt after a period of inactivity often fails with `P1001` regardless of platform — a simple retry (or a throwaway `pg` connection first) resolves it. Don't mistake this for the binary-blocking issue above; the binary-blocking issue is 100% reproducible and persists across retries, the cold-start issue resolves itself after one retry.
- This is a workaround for THIS machine's local security posture, not a code issue — nothing about it should be "fixed" in the repo itself. If a future session is on a different machine (or this one after an IT-approved fix), these DB-touching commands may just work natively on Windows; try that first and only fall back to the WSL steps above if `P1001` reappears immediately (not just on the first cold-start attempt).

### Task 3.0 notes

- **NextAuth type augmentation gotcha**: `declare module "next-auth/jwt" { interface JWT {...} }` does **not** work — `next-auth/jwt.d.ts` re-exports the `JWT` interface from `@auth/core/jwt` via a wildcard `export *`, and TypeScript declaration merging only attaches to the module where an interface is genuinely declared. Wildcard re-exports don't support this; named re-exports (`export type { Session, User } from "@auth/core/types"`, which is what `next-auth`'s main entry actually uses) do. Fixed by augmenting `"@auth/core/jwt"` directly in `src/types/next-auth.d.ts`. Without this fix, any property read off the JWT token (`token.role`) silently types as `unknown` (JWT extends `Record<string, unknown>`), not a "property doesn't exist" error — easy to miss.
- Auth is built with Server Actions (`useActionState` + `action={formAction}`), matching Next.js's own current App Router guidance (checked `node_modules/next/dist/docs/01-app/02-guides/authentication.md` before building) — not client-side `signIn()` from `next-auth/react`, which would've needed a `SessionProvider` wrapping the app for no real benefit here.
- Route protection uses `src/proxy.ts` (Next 16 renamed `middleware.ts` → `proxy.ts`), wrapping NextAuth's `auth()` helper: unauthenticated users hitting a non-public route redirect to `/login`; authenticated users hitting `/login` or `/signup` redirect to `/`. Verified in-browser both directions.
- Full auth flow (login success, login failure with wrong password, signup creating a real `User` row + auto-sign-in, sign-out, 404 on an unknown project id, tab switching on the Project Detail shell) verified end-to-end in the browser tool against the real dev database — not just typechecked/linted.
- **Browser-tool artifact, not a real bug**: at a resized mobile (375px) viewport, the automation tool's `form_input` (sets DOM `.value` directly) and a separate `left_click` on the submit button raced — the dev server logged the submitted `FormData` as empty even though a direct JS check confirmed the DOM inputs held the correct values at that moment. This only happened after `resize_window` + a fresh navigate in this same tool session; the identical login flow worked correctly and repeatedly at desktop size, and the login page's markup has zero breakpoint-conditional differences to begin with. Concluded this is a tool synchronization quirk, not a responsive-design defect — didn't chase it further. If a future session hits the same symptom (empty FormData despite correct-looking DOM state) after a viewport resize, this is why.
- Component tests needed `afterEach(cleanup)` added manually to `tests/setup.ts` (imported from `@testing-library/react`) — React Testing Library's automatic cleanup relies on a global `afterEach` hook that only self-registers when Vitest's `test.globals: true` is set, which this project doesn't use (tests import `describe`/`it`/`expect` explicitly from `"vitest"` instead). Without it, DOM nodes accumulated across tests in the same file and caused "found multiple elements" failures, plus a spurious "form was unexpectedly submitted" React warning from stale unmounted-but-still-attached forms.
- Installed `zod` (v4) for form validation (signup) — also intended for later use validating Claude's structured JSON outputs in the agent tasks (4.0+), matching CLAUDE.md's "never parse prose, always validate structured output" rule.
- Design tokens: deliberately no dark mode for now (single light "clean/efficient/professional" palette via Tailwind v4 `@theme` in `globals.css`) — not required by the PRP, and simpler for a first pass.

### Task 4.0 notes

- **Anthropic API key provided by the user** and added to `.env.local` (gitignored). Verified working with a live smoke test before building the rest of the agent on top of it.
- **Model choice: `claude-opus-5`.** Loaded the bundled `claude-api` skill before writing any Anthropic SDK code (per its own trigger rule whenever Claude/Anthropic is named) — it's current on model IDs, structured-output syntax, and API shape, none of which should be trusted from training-data recall given how fast this surface moves.
- **Structured output via `output_config.format` + `zodOutputFormat()` + `client.messages.parse()`**, not manual JSON parsing or a tool-call workaround. This directly satisfies CLAUDE.md's "every agent returns structured JSON, never free text the app has to parse" — validation is enforced by the SDK against the same Zod schema used for the TypeScript types, and `message.parsed_output` is already the typed, validated object.
- **Consolidated docx/pdf/pptx parsing onto a single library (`officeparser`)** instead of CLAUDE.md's suggested `mammoth` + `pdf-parse` + a separate pptx library. `officeparser` actively maintains all three formats (plus more) with one consistent API (`OfficeParser.parseOffice(buffer)` → `ast.to("text")`), which is simpler and more consistent than juggling three different library APIs — CLAUDE.md's library list was explicitly "or similar" for each. Installed, then uninstalled `mammoth`/`pdf-parse` once this was confirmed sufficient.
- **Intake Agent is 3 real Claude calls, not more, not fewer**: classify → extract → draft clarification email. The Project Position Document and Set-Up Checklist are **not** separate Claude calls — the Position Document is a direct structural rendering of the extraction call's output (no extra AI step needed), and the Set-Up Checklist is a fixed, deterministic template (`generateSetupChecklist()`, no AI call at all) per the task list's own wording ("Workbook, folder, job code, PM assignment, Teams channel" — a fixed list, not something to extract from the brief).
- **Synchronous request, not a background job**: `createProjectAction` runs all 3 Claude calls inline within the Server Action before redirecting. This is a real latency/timeout tradeoff (Vercel serverless function limits) accepted deliberately for MVP simplicity — verified in-browser that the full round trip (with real Claude calls) completes in well under any Vercel timeout tier for a normal-length brief, and the form shows a "Running Intake Agent…" pending state throughout. If briefs grow much longer or this needs to scale, revisit as a background job in Level 2.
- **Checklist items are dual-tracked by design**: a `Document{type: CHECKLIST}` + `DocumentVersion` stores the immutable generated-checklist artifact (for version history later), while separate `ChecklistItem` rows (one per item) are what task 7.4 will wire up for interactive tick-boxes. Both are created together in the same transaction when a project is created. Rendered read-only for now (no tick interaction yet — that's explicitly task 7.4's job, not 4.8's).
- **Full flow verified end-to-end in the browser against the real Claude API and real Neon database**: pasted a genuine-shaped brief (Coffee loyalty app, with both a genuine gap and a client-flagged TBC item) through `/projects/new`, watched Stage 1 → Stage 2 transition correctly on the taxonomy browser, and confirmed the project page rendered a well-structured clarification email, a position document with properly separated "genuine gap" vs "client-flagged" sections, and the checklist — not just typechecked/linted.
- Prisma's typed `Json` write fields (`DocumentVersion.content`) reject a pre-typed object (like our own `SetupChecklist` interface) unless cast to `Prisma.InputJsonValue` — Prisma's `InputJsonObject` requires an index signature that named interfaces don't structurally satisfy on their own. Fresh object literals inferred inline don't hit this; a named type assigned through a function return does. Cast with `as unknown as Prisma.InputJsonValue` at the call site when this comes up again (e.g. future Draft Scope Document / Deliverables+Services writes).

### Ad-hoc UI restructure (2026-08-07, before starting task 5.0)

- **User request, not a scheduled task**: the user shared a mockup of a richer project page — a single numbered step list (all 10 pipeline stages visible, expand/collapse, status badges, inputs/outputs per step, inline "Run Agent"/"Submit Notes" controls) plus an integrated chatbot + knowledge-upload sidebar, replacing the 4-tab layout built in task 3.0. Discussed timing with the user first (do it now vs. wait for task 7.0/8.0) — recommended doing it now since only 2 of 10 steps had real logic behind them, so the cost of swapping the shell was at its lowest; the user agreed and asked me to prepare the structural update (styling not a concern, just the layout shape).
- **Replaced** `ProjectDetailTabs.tsx` (and its test) with three new components: `WorkflowStepList.tsx` (client component — expand/collapse state, one step open at a time, defaults to the first non-`COMPLETE` step), `ChatPanel.tsx` (static sidebar placeholder for tasks 8.1–8.4), and `ProjectWorkflow.tsx` (Server Component — builds each step's content, reusing the existing `ClarificationEmailView`/`PositionDocumentView`/`ChecklistView` from task 4.0 unchanged, and lays out the two-column grid: `grid-cols-1` on mobile, `lg:grid-cols-[2fr_1fr]` on desktop).
- **New `src/types/workflow.ts`**: `WorkflowStep`/`StepStatus` types, plus a `STEP_KIND_BY_STAGE` map (`AGENT` vs `HUMAN_INPUT`) used purely for the step subtitle/action-button label — e.g. Stage 3 "Get Clarifications" and Stage 5 "Review with Specialist Leads" are human note-capture touchpoints, not agent runs, so they'll get a "Submit Notes" control instead of "Run Agent" once tasks 5.1/6.1 wire them up.
- **Step ↔ artifact mapping is a presentation-layer decision, not a schema change**: `DocumentVersion.stageNumber` is still written as `1` for all three Stage-1 artifacts (unchanged from task 4.0's `actions.ts`), but the Clarification Email is *displayed* under Step 2 ("Clarification Email Sent") since that's what that stage name actually describes. This mapping is hardcoded in `ProjectWorkflow.tsx` (`contentByStage`) — no DB migration needed, and nothing about the write path changed.
- **All 10 Stage rows now render**, most as inert "Coming in task X.X" / "Level 3 (post-MVP)" placeholders with a disabled action button — same honesty convention as the old tab placeholders (never implies functionality that isn't built).
- **Page query changed**: `/projects/[projectId]/page.tsx` now also fetches all `Stage` rows with each one's own `ProjectStageStatus` for *this* project only (`stageStatuses: { where: { projectId } }`), defaulting to `NOT_STARTED` when no status row exists yet (true for stages 3-10 on every project so far, since `createProjectAction` only creates rows for stages 1-2).
- Verified in-browser against real seeded/created projects at both desktop and mobile widths (mobile stacks the sidebar below the step list via the `grid-cols-1` default) — not just typechecked. Test suite net-neutral at 28 tests (removed 4 `ProjectDetailTabs` tests, added 3 `WorkflowStepList` + 1 `ChatPanel` tests). Full typecheck/lint/build all clean.

### Task 5.0 notes

- **Design decision made before writing code, not left implicit**: the PRP describes "paste client reply → extraction updates Position Doc → Triage Agent generates Draft Scope Document" as one continuous flow, but the new step-console UI (from the ad-hoc restructure above) explicitly separates `HUMAN_INPUT` steps from `AGENT` steps with their own controls. Chose **two separate manual actions** rather than auto-chaining: submitting clarification notes (Step 3) runs extraction and stops there; running the Triage Agent (Step 4) is a distinct, explicit "Run Triage Agent" button the PM clicks once ready. This matches the mockup's "Inputs ready — Run Agent" pattern (giving the PM visibility/control between steps) and follows the same task-list split (5.2 extraction vs. 5.4 Triage as separate concerns).
- **Stage-completion bundling**: submitting clarification notes marks **both** Stage 2 ("Clarification Email Sent") and Stage 3 ("Get Clarifications") complete in the same action, then sets Stage 4 ("Triage") to `IN_PROGRESS`. Reasoning: Stage 2 was left `IN_PROGRESS` indefinitely after Intake (there's no in-app event for "the AM actually sent the email" — that happens outside the platform), so receiving a client reply is the first evidence, in-app, that the email was sent. Retroactively completing it there avoids a stage that would otherwise never resolve.
- **New agent modules**, both following the exact `intake-agent.ts` conventions (typed error class wrapping `Anthropic.RateLimitError`/`APIError`, `messages.parse()` + `zodOutputFormat()` structured output, `claude-opus-5`, one Claude call each — no unnecessary decomposition):
  - `src/services/agents/clarification-extraction.ts` — `extractClarificationUpdate(currentPositionDocument, clarificationNotes)`. Reuses `PositionDocumentFieldsSchema` from task 4.0 unchanged (same shape in, same shape out) rather than inventing a new type. Prompted explicitly to move resolved items between `whatWeKnow`/`whatWeNeedToFindOut`/`clientFlaggedOpenItems` rather than just appending, so the three-way split stays meaningful after multiple rounds of clarification.
  - `src/services/agents/triage-agent.ts` — `generateDraftScopeDocument(positionDocument)`. New `src/types/triage.ts` schema (`DraftScopeDocument`: objectives, deliverables, milestones, rolesAndResponsibilities, budget, assumptionsAndConstraints, `flaggedGaps`). Prompted explicitly to produce a complete draft regardless of gaps and to carry every remaining gap into `flaggedGaps` rather than silently dropping it — directly implements the PRP's "proceed regardless of remaining gaps, but flag them clearly for specialists."
  - Both smoke-tested against the real Claude API with realistic data before writing any UI — output quality confirmed genuinely good (correct resolve/carry-forward logic, sensible assumptions explicitly stated, comprehensive flagged-gaps list) before building on top of them.
- **New `src/app/(dashboard)/projects/[projectId]/actions.ts`**: `submitClarificationNotesAction` and `runTriageAgentAction`, both `(projectId, prevState, formData) => ActionState`, bound via `.bind(null, projectId)` in their respective client components so they work with `useActionState`'s fixed 2-arg signature. Both use `revalidatePath` (not `redirect`) since they mutate data on the *current* project page rather than navigating away — matches Next.js's standard pattern for same-page Server Action mutations.
- **UI**: `ClarificationNotesForm.tsx` (Step 3 — textarea + submit, replaced by read-only notes + updated Position Document once a `CLARIFICATION_REPLY` TouchpointNote exists), `RunTriageAgentButton.tsx` (Step 4 — button shown only while Stage 4 is `IN_PROGRESS` and no Draft Scope Document exists yet), `DraftScopeDocumentView.tsx` (gaps rendered in a warning-styled card *above* the rest of the document, not buried at the bottom — the PRP's "flagged clearly" requirement, taken literally).
- **ESLint config change**: added `argsIgnorePattern: "^_"` / `varsIgnorePattern: "^_"` to `@typescript-eslint/no-unused-vars`. `runTriageAgentAction` doesn't need `prevState` or `formData` but must keep them for the `useActionState` signature — the underscore-prefix convention was already in informal use (task 4.0's `_prevState`) but only now actually triggered warnings, since that action left the *last* parameter (`formData`) used, and the default rule only flags unused args after the last *used* one.
- **Found and fixed a real test-coverage gap while writing task 5.7's tests**: the ad-hoc UI restructure had removed the old `ProjectDetailTabs` test (which exercised real artifact rendering) and replaced it with `WorkflowStepList.test.tsx`, which only ever tests synthetic `<p>` content — so `ProjectWorkflow.tsx`'s actual per-step content logic (form vs. read-only, button vs. document view) had zero coverage. Added `tests/components/ProjectWorkflow.test.tsx` to close that gap, mocking the new `actions.ts` module (avoids pulling `@/lib/prisma`/`@/lib/auth` into the component test). Discovered in the process that `WorkflowStepList`'s expanded-step state is computed once at mount and then preserved as local state on re-render (React reconciliation keeps client component state across a Server Component `revalidatePath` refresh) — test fixtures need every *earlier* stage marked `COMPLETE` for the stage under test to actually be the auto-expanded one, or its content never mounts.
- **Full flow verified end-to-end in the browser against the real Claude API and real Neon database**: on the seeded "Loyalty App Relaunch" project, submitted a realistic client reply (resolved 3 of 3 open items) → confirmed Steps 1-3 all flipped to Complete, Position Document version 2 correctly moved resolved items into "What We Know" and emptied the now-resolved lists, Step 4 unlocked → clicked "Run Triage Agent" → confirmed Step 4 completed with a comprehensive Draft Scope Document (flagged gaps clearly listed first) → confirmed Step 5 unlocked and the taxonomy browser showed "Stage 5". Checked at both desktop and via console for errors (none beyond the known benign `pg` SSL deprecation notice).
- Test suite grew from 28 to 39 (7 new agent-module tests mocking `@anthropic-ai/sdk`, 5 new `ProjectWorkflow` tests). Typecheck, lint, full build all clean.

### Ad-hoc project summary bar (2026-08-07, before starting task 6.0)

- **User request, not a scheduled task**: after showing the user the app running live, the user shared the header/summary bar from their original mockup (Job #, Kick-off date, Target date, PM, status badge, "Open WorkBook"/"View SOW Draft" buttons) and asked for the summary-detail portion (not the WorkBook/SOW buttons, which need integrations that don't exist — Level 2/3 scope) to be added to the real project page, both UI and backend.
- **New nullable `Project` fields**: `jobCode`, `kickOffDate`, `targetCompletionDate`, `projectManagerId` (FK → `User`, `onDelete: SetNull`). Reverse relation `User.projectsManaged`. None of these are populated by any agent — they're operational metadata the PM sets directly, distinct from the Set-Up Checklist's "Assign job code"/"Assign Project Manager" tick-boxes (which remain simple booleans until task 7.4 wires up real persistence; deliberately did **not** try to connect the two now — that would have pulled in checklist-item-to-data-field wiring ahead of its own task).
- Migration `20260807101251_add_project_summary_fields` applied via the documented WSL workaround (see task 2.0 notes above) — worked first try this time, no cold-start retry needed.
- **`seed.ts` reordered**: user creation now happens *before* the demo project upsert, so the demo project ("Fizzy Summer Launch") can set `projectManagerId` to Priya Mehta's real id. Added sample values (`FIZ-2026-014`, kick-off 1 Jun 2026, target completion 15 Sep 2026 — chosen to align with the existing seeded brief text's "launch by end of Q3"). The upsert's `update` clause had to be changed from `{}` to actually set these fields — the project already existed from earlier seeding, and an empty `update` object means "don't touch existing rows," so the first re-seed attempt silently did nothing until this was fixed.
- **Found and fixed a real, exploitable security issue while re-running `npm install` from WSL**: `npm audit` flagged `pdfjs-dist` (transitive dependency of `officeparser`, which parses every user-uploaded PDF brief) for a high-severity "arbitrary JavaScript execution upon opening a malicious PDF" advisory — directly relevant to our actual attack surface (untrusted file uploads). `npm audit fix --force`'s suggested fix was to downgrade `officeparser` 7.5.1 → 6.0.7 (a real regression risk to the parsing code built in task 4.0). Instead, checked `officeparser@7.5.1`'s dependency and found it pins `pdfjs-dist` to an exact vulnerable version (`6.1.200`, no version range) while the fixed release (`6.2.108`) is only a patch bump away — added an npm `overrides` entry forcing `pdfjs-dist` to `6.2.108` while keeping `officeparser` at its current version. `npm audit` now reports 0 vulnerabilities. This wasn't part of what was asked, but it's a direct, live vulnerability in code already accepting untrusted input, so fixed it immediately rather than deferring.
- **New toolchain gotcha discovered and documented**: switching between WSL (`npm install`, needed for the migration/seed workaround) and native Windows (`npm install`, needed for everything else) **does** clobber the other platform's optional-dependency native binaries — contradicts what task 2.0's notes assumed ("both binaries now coexist... switching back and forth is safe"). Hit this twice in a row today: first `tsx`/`esbuild` broke on the WSL side (needed `npm install` from WSL to restore), then `vitest`'s `rolldown` bundler broke on the Windows side afterward (needed a second `npm install`, this time from Windows, to restore). **Going forward: after ANY `npm install` from WSL, immediately run `npm install` again from native Windows before doing anything else** (build, test, dev) — don't assume the previous session's binaries survived. The earlier assumption in the task 2.0 notes was wrong; corrected here.
- **UI**: `ProjectSummaryBar.tsx` — a view/edit toggle card (not a separate page/modal) rendered between the breadcrumb and the step list. View mode shows a status badge (`ACTIVE`/`COMPLETE`, computed server-side from whether every stage's `ProjectStageStatus` is `COMPLETE`) plus the four fields with "Not yet set" fallbacks, matching the existing fallback convention from the Position Document view. Edit mode swaps in a form (text input, two date inputs, a `<select>` of `DELIVERY`-role Users only — matching this project's role semantics, `am@projectbrain.test`/Alex Morgan is `CLIENT_ENGAGEMENT` and intentionally excluded from the PM dropdown) bound to a new `updateProjectSummaryAction`, closing back to view mode on successful save.
- Verified end-to-end in the browser against the real Neon database: seeded data displays correctly on "Fizzy Summer Launch", "Not yet set" fallbacks display correctly on "Loyalty App Relaunch" (no summary data), edited and saved a job code change and confirmed it persisted and re-rendered, checked at mobile width (wraps cleanly via `flex-wrap`). Test suite grew from 39 to 43 (`tests/components/ProjectSummaryBar.test.tsx`, mocking the actions module same as `ProjectWorkflow.test.tsx`). Typecheck, lint, full build all clean.

### Task 6.0 notes

- **Confirmed the file-structure naming decides the shape**: CLAUDE.md names this file `specialist-review-extraction.ts` (an *extraction step*, like `clarification-extraction.ts`), not `specialist-review-agent.ts`. That naming is the actual design signal — Stage 5 is a **single combined action** (paste feedback → extraction runs immediately → document produced → stage completes), matching Stage 3's pattern, *not* Stage 4's two-step "submit notes, then separately click Run Agent" split. Correctly anticipated this in the previous checkpoint's "next up" note before writing any code.
- **Services table modeled as a fixed object, not an array**: `services: { experienceCreative, business, architecture, techAndData, orchestration, other }`, each `{ involvement: string }` (`other` also carries `label: string`). Considered `services: ServiceRow[]` with a `capability` enum first, but structured-output JSON Schema doesn't support an exact-length array constraint (`z.array(...).length(6)` gets validated client-side only, not enforced on the model) — a fixed object with named keys is enforced by the schema itself (every key is required) and renders as a deterministic 6-row table via `SERVICE_ROWS` (a plain array of `{key, label}` pairs, iterated in the view) without needing to trust the model's array ordering.
- **Every one of the 5 fixed non-"Other" capabilities is always present, explicitly marked "Not required" when unneeded** — this was prompted deliberately (`extractDeliverablesAndServices`'s prompt spells out "write 'Not required' if a capability isn't needed") and confirmed working in the live smoke test (Architecture correctly came back "Not required" for a pure marketing campaign with no build work).
- **The "Other" label is genuinely editable now**, not deferred like the Set-Up Checklist tick-boxes — task 6.3 called this out explicitly, unlike task 4.0/5.0's general "interactive editing lands later" pattern. `EditableOtherLabel.tsx` is a small click-to-edit component (button → text input + Save/Cancel) bound to a new `updateOtherServiceLabelAction`. **This is an in-place `documentVersion.update()`, not a new version** — per CLAUDE.md's "version at stage transitions, not on every edit," a label correction isn't a stage-transition artifact.
- **Stage 5→6 unlocks Stage 6 (Estimation Kick Off) as `IN_PROGRESS`**, continuing the same pattern every prior stage completion has followed, even though Stage 6 itself is Level 3/post-MVP and still renders a placeholder. Kept the workflow visually honest about how far the pipeline has actually progressed rather than freezing the unlock chain at Stage 5.
- Smoke-tested `extractDeliverablesAndServices` against the real Claude API before writing any UI (same discipline as tasks 4.0/5.0) — output correctly resolved capability-by-capability from freeform feedback, invented a sensible combined "Other" label ("Media, Production, and Legal & Compliance") when multiple leftover concerns didn't fit the fixed five, and correctly carried forward only the gaps the feedback didn't address.
- **Full flow verified end-to-end in the browser against the real Claude API and real Neon database**: on "Loyalty App Relaunch" (already at Stage 5 from task 5.0's testing), pasted realistic specialist feedback (Design/Engineering/Legal input, explicitly ruling out a dedicated architecture specialist) → confirmed Stage 5 completed, the Deliverables + Services Document rendered with all 6 service rows (Architecture correctly "Not required"), Open Questions/Risks and Outstanding Gaps Carried Forward both populated sensibly → confirmed Stage 6 unlocked on both the project page and the taxonomy browser ("Stage 6") → clicked "Edit label" on the AI-generated "Legal & Data Privacy" row, changed it to "Legal & Compliance", saved, confirmed it persisted after re-render.

### Task 7.0 notes

- **No agent work this task, no schema changes** — this task is entirely about surfacing data the pipeline already produces (per CLAUDE.md's Phase 4 framing: "read across whatever the pipeline has produced so far"). `ChecklistItem` already had `isComplete`/`completedAt` columns sitting unused since task 2.0; no migration needed for 7.4.
- **Stage Tracker (`StageTracker.tsx`) is a new, separate component from `WorkflowStepList`**, not a replacement — it's a compact horizontal at-a-glance strip (numbered/checked nodes + connecting line, all 10 stages, stages 6-10 dashed and labeled "Later" since they're modeled but not built) rendered above the existing detailed accordion in `ProjectWorkflow.tsx`. The PRP's "visual current-stage status per project" (Level 1 feature list) reads as its own artifact distinct from the accordion, which is itself more of a workspace than a tracker.
- **Found and fixed a real horizontal-overflow bug surfaced by the Stage Tracker**: `StageTracker`'s row of 10 nodes is wider than most viewports by design (`min-w-max` inside an `overflow-x-auto` wrapper, meant to scroll internally). But the dashboard shell's `<main className="flex-1 p-6">` (`src/app/(dashboard)/layout.tsx`) is a flex item with no `min-width: 0`, so instead of the tracker scrolling within its own box, the flex item refused to shrink below its widest descendant's content size and the *entire page* (header included) gained a horizontal scrollbar. Fixed with one class: `min-w-0` added to `main`. Caught by actually loading the page in the browser at the Browser pane's default (sub-768px) width — would not have been caught by typecheck/lint/tests, another data point for why the in-browser verification step in CLAUDE.md's testing strategy matters even for "just visual" additions.
- **Outputs Library and Version History are two new pages, not sections bolted onto the existing project page** (`/projects/[projectId]/outputs` and `/projects/[projectId]/outputs/[documentType]`) — the project page was already dense (summary bar + step accordion + chat sidebar), and "library"/"history" read as their own destinations you navigate to, not inline content. Linked from the project page via a small "Outputs Library →" link next to the breadcrumb.
- **Version History reuses the existing per-artifact View components** (`ClarificationEmailView`, `PositionDocumentView`, `ChecklistView`, `DraftScopeDocumentView`, `DeliverablesServicesDocumentView`) rather than building new renderers — added a small dispatcher, `DocumentVersionContent.tsx`, that Zod-parses a version's raw JSON per its `DocumentType` and renders the matching view, falling back to a friendly "couldn't be read" message on a schema mismatch (defensive only — versions are always agent-written, this should never actually trigger). Added a missing `SetupChecklistSchema` to `src/types/intake.ts` (the `CHECKLIST` document type never had one, since nothing needed to re-validate it until now) and a `readOnly` prop on `DeliverablesServicesDocumentView` so old/non-latest versions in history don't render a live, clickable `EditableOtherLabel` — editing only makes sense on the current version.
- **Version History uses a native `<details>`/`<summary>` disclosure per version, no client-side JS**: the most recent version is `open` by default, older ones collapsed. Kept it a pure Server Component this way rather than adding a client-side expand/collapse component for what's a simple, no-state-to-manage interaction.
- **Checklist tick-boxes are now genuinely interactive** (`EditableChecklist.tsx`, replacing `ChecklistView` on the live Stage 1 step — `ChecklistView` itself stays read-only and is now used for the immutable `CHECKLIST` Document artifact's version history, which is a frozen snapshot from Stage 1 and was never meant to be tickable). Each item is its own tiny `<form>` bound to a new `toggleChecklistItemAction`, auto-submitting on checkbox change via `form.requestSubmit()` — deliberately not a single "Save checklist" button, to match the "tick items off as you go" mental model and avoid losing a tick if the PM navigates away mid-session.
- **`toggleChecklistItemAction` scopes its update by both `id` *and* `projectId`** (`updateMany({ where: { id: itemId, projectId }, ... })`) even though `id` alone is already a unique key — cheap insurance against a stale/tampered `itemId` from one project's form ever touching another project's row, consistent with the project-scoping discipline CLAUDE.md mandates for the chatbot (task 8.0) applied here too even though it's not a hard requirement for this feature.
- **Full flow verified end-to-end in the browser against the real Neon database**: on "Loyalty App Relaunch," confirmed the Stage Tracker renders all 10 stages with correct check/in-progress/pending states and "Later" labels on 6-10; visited the Outputs Library and saw all 5 real documents with correct version/stage/date/author metadata; opened the Project Position Document's history and confirmed both real versions render (v2 open by default, v1 collapsed, each with its own correct content); ticked "Assign job code" on the live checklist, reloaded the page, confirmed it persisted checked, then unticked it back to leave demo data clean. Confirmed the `min-w-0` overflow fix by checking the same page before and after at the Browser pane's narrow default width — page-level horizontal scroll gone, tracker's own internal scrollbar still works.
- Test suite grew from 51 to 63 (`StageTracker.test.tsx`, `EditableChecklist.test.tsx`, `DocumentVersionContent.test.tsx` — the last one covers the rendering logic shared by both the Outputs Library and Version History pages, since neither page itself is unit-tested per this codebase's existing convention of not testing Server Components that call Prisma directly). Typecheck, lint, full production build all clean.
- Test suite grew from 43 to 51 (3 `specialist-review-extraction` agent tests, 2 new `ProjectWorkflow` Step 5 tests, 3 `EditableOtherLabel` tests). Typecheck, lint, and production build all clean.

### Ad-hoc: checklist also shown in the sidebar (2026-08-07, immediately after task 7.0)

- **User request, not a scheduled task**: the user liked the interactive Set-Up Checklist and asked for it to also appear in the right-hand sidebar, below where the chatbot would land, so it's visible regardless of which workflow step is expanded — not just when Step 1 happens to be open.
- **Rendered in both places at once** (Step 1's own content and the sidebar), rather than moved out of Step 1 — the user explicitly asked for both when offered a choice between "sidebar only" and "both".
- **Found and fixed a real cross-instance sync bug this surfaced**: with two live `EditableChecklist` copies bound to the same server data, toggling one didn't reliably update the other's checkbox without a full page reload. Root-caused through live browser testing (not obvious from code review alone) to `useActionState`'s pending-state transition re-rendering the toggled instance with its still-stale `checked` prop mid-flight, plus an uncontrolled/`useEffect`-resynced checkbox never being told to re-check itself from a sibling's prop update. Fixed by keying each `EditableChecklistItem` on `` `${item.id}-${item.isComplete}` `` (not just `item.id`) — forcing a full remount, and therefore a fresh `useActionState` and freshly-derived `checked`, the instant either copy's toggle completes and its item's `isComplete` prop changes. Verified in the browser toggling from each copy in turn and confirming the other updated without a reload.
- Test suite grew from 63 to 66 (`StageTracker`/`EditableChecklist`/`ProjectWorkflow` test updates plus a new `EditableChecklist` re-sync test simulating the exact prop-change scenario). Typecheck, lint, production build all clean.

### Task 8.0 notes

- **`KnowledgeItem` needed zero schema changes** — the model (`type`, `title`, `content`, `originalFileName`, `uploadedById`) has sat fully defined since task 2.0, unused until now.
- **Reused `parseDocumentToText` from task 4.2 as-is**, per the task list's explicit instruction — including its brief-flavored error type (`UnsupportedBriefFormatError`) and docstring, which read generically enough not to warrant a rename for this second caller. `uploadKnowledgeItemAction` mirrors `createProjectAction`'s exact paste-vs-upload validation shape (reject if neither or both are provided).
- **Deliberate, documented exception to "DB fetch lives in `actions.ts`, agents are pure functions"**: every other agent module receives its input as plain data from the calling Server Action, which does its own Prisma reads first. The chatbot inverts this — `assembleProjectContext(projectId)` lives in `src/services/agents/chatbot.ts` and does its own DB reads. Reasoning: CLAUDE.md's task list names "context assembly strictly filtered by `project_id`" (8.4) and "explicit cross-project isolation check" (8.5) as their own first-class deliverables, distinct from "then Claude call" — that only makes sense if context assembly is itself a directly named, directly testable unit. Splitting `chatbot.ts` into `assembleProjectContext` (DB-only, real-DB tested) and `answerQuestionFromContext` (pure Claude call, mock-tested exactly like every other agent) gets both: a defensible isolation test and full consistency with the rest of the codebase's agent-testing conventions.
- **The isolation check is two layers, not one**: the Prisma `where: { projectId }` on every query, *and* an explicit `.filter(row => row.projectId === projectId)` re-assertion on every result before it's folded into context — redundant against the WHERE clause today, but a cheap, visible guard against a future unscoped query anywhere in this function ever leaking data across projects. `assembleProjectContext` pulls from Documents, ChecklistItems, TouchpointNotes, *and* KnowledgeItems — matching `tasks.json`'s fuller description of 8.4, caught on a re-read after an initial pass had only wired in three of the four (ChecklistItems were missing until a docs cross-check caught it, before this checkpoint).
- **Isolation is proven by a real-DB integration test, not a mock**: `tests/chatbot-isolation.test.ts`, same convention as `tests/schema.test.ts` (throwaway Hub, cascade-deleted in `afterAll`) — two Projects under one Client, each seeded with a uniquely-markered Document/ChecklistItem/TouchpointNote/KnowledgeItem, asserting `assembleProjectContext` for Project A contains only A's markers and Project B's context contains only B's, in both directions. This is what CLAUDE.md's testing strategy explicitly calls for, and it's the kind of property mocking can't actually prove.
- **No chat-history persistence** — there's no `ChatMessage` model and the PRP never asks for one; conversation turns live in the `ChatPanel` client component's own React state and vanish on reload. The chatbot's *answers* are always freshly grounded in current DB state regardless.
- **Chatbot answer is still structured JSON** (`{ answer: string }` via `zodOutputFormat`), not raw text, even though nothing downstream parses it — kept for consistency with CLAUDE.md's blanket "every agent returns structured JSON" rule and because it costs nothing.
- **Smoke-tested `answerQuestionFromContext` against the real Claude API before wiring up the UI** (same discipline as every prior agent) — hit one real bug in the smoke-test script itself, not the product: a static `import` of `chatbot.ts` gets hoisted above a `dotenv.config()` call that appears earlier in the same file, so `@/lib/anthropic`'s module-level `new Anthropic({apiKey: ...})` singleton was constructed before `ANTHROPIC_API_KEY` was ever loaded, failing with "Could not resolve authentication method." Fixed by switching the smoke-test script to a dynamic `await import(...)` after `config()` runs. Not a product bug — the real app loads env vars via Next.js's own `.env.local` handling long before any module evaluates — but worth remembering for the next throwaway smoke-test script on this machine.
- **Full flow verified end-to-end in the browser against the real Claude API and real Neon database**: uploaded a pasted knowledge-item note to "Loyalty App Relaunch" (descoping the referral feature) and confirmed it listed and persisted; asked the chatbot a two-part question and got an answer that correctly synthesized the Position Document/Draft Scope/Deliverables documents *and* the new note, explicitly flagging the resulting conflict between the older documents (referral in scope) and the newer note (referral descoped) rather than silently picking one; ticked a checklist item and confirmed a follow-up question about checklist progress correctly reflected it. Then, on the unrelated "Fizzy Summer Launch" project (same Hub, different Client), asked an equivalent question and got "no context available" with zero mention of Coffee's £120k budget or referral decision — live confirmation of the isolation the integration test proves mechanically.
- Test suite grew from 66 to 77 (`tests/services/chatbot.test.ts` — 3 tests mocking Anthropic; `tests/chatbot-isolation.test.ts` — 2 real-DB isolation tests; `tests/components/ChatPanel.test.tsx` rewritten for the now-interactive chat UI — 3 tests; `tests/components/KnowledgeUpload.test.tsx` — 4 new tests; `ProjectWorkflow.test.tsx` updated for the new `knowledgeItems` prop and action mocks). Typecheck, lint, and production build all clean.

### Task 9.0 notes

- **9.1 Responsive pass**: checked the taxonomy browser, login/signup, project creation, project detail (summary bar/Stage Tracker/step list/sidebar), Outputs Library, and Version History at mobile (375px), tablet (768px), and desktop widths. All clean — no overflow, correct single-column-to-grid stacking at the `lg:` breakpoint, Stage Tracker's own horizontal scroll never leaks to the page (confirming task 7.0's `min-w-0` fix holds).
- **9.2 Accessibility pass** found and fixed 4 real gaps (placeholder-only inputs with no programmatic accessible name, a WCAG 1.3.1/4.1.2 issue): the New Project brief textarea/file input (now a real `<label htmlFor>`), the Knowledge Upload title/notes/file fields and the chatbot's question input (`aria-label`, since these compact widgets already have contextual headings and a full visible `<Label>` per field would be clutter — matching the precedent `EditableChecklist`'s checkboxes already set), and the "Other" service label editor input. Also added `aria-live="polite"` to the chatbot's message list so screen readers announce new answers. Color contrast checked by hand against WCAG AA for every foreground/background pair in `globals.css`'s theme tokens — worst case (`muted-foreground` on `background`) is ~4.55:1, still comfortably over the 4.5:1 minimum. Keyboard nav confirmed logical (DOM order matches visual order throughout, no tabindex hacks). Fixing these surfaced a genuine, pre-existing `react-hooks/set-state-in-effect` lint finding in `ChatPanel.tsx` (unrelated to the a11y edit that revealed it) — fixed by replacing the `useEffect` with React's documented "adjust state during render" pattern instead of the effect-based one.
- **9.3 E2E happy-path test** (`tests/stage-1-5-happy-path.test.ts`): calls the **real** Server Actions in sequence — `createProjectAction` → `submitClarificationNotesAction` → `runTriageAgentAction` → `submitSpecialistFeedbackAction` — against the real dev database, exactly as a PM would trigger them from the UI. Only the Next.js/Anthropic framework boundaries are mocked (`@anthropic-ai/sdk`'s `messages.parse` returning 6 canned responses in call order; `next/cache`'s `revalidatePath` as a no-op; `next/navigation`'s `redirect` as a throw-and-catch, since `createProjectAction` redirects after its transaction commits; `@/lib/auth`'s `auth()` resolving `null`, since none of these actions require a real session to run). This is a genuinely different, more valuable test than the existing per-agent unit tests — it's the first thing in this repo that proves the actual orchestration code (stage transitions, document versioning, `currentStageNumber` bookkeeping) works end-to-end, not just that each agent function individually returns the right shape. Passed on the first real run.
- **9.4 Secrets audit**: `git grep` across all tracked files for API-key/connection-string/secret-shaped patterns found nothing but the intentional placeholder in `.env.local.example`; confirmed via `git log --diff-filter=A` that `.env.local` was never added in any commit; confirmed every secret read in the app goes through `process.env` (`src/lib/anthropic.ts`, `src/lib/prisma.ts`, `prisma.config.ts`) with no literal fallback. `.env.local.example` matches CLAUDE.md's required var list exactly.
- **Found and fixed a real, separate security issue while auditing**: `NEXTAUTH_SECRET` in `.env.local` was still the literal placeholder text `"replace-with-generated-secret"` from `.env.local.example` — never actually replaced with a real generated value, despite progress.md's task 1.0 notes flagging it as still-a-placeholder back then and it apparently being forgotten since. Generated a real one (`crypto.randomBytes(32).toString('base64')`) and replaced it in both `.env.local` and (see below) the Vercel production env var, before that placeholder could ever be relied on for a real session.
- **Incident: `.env.local` was displayed in full in this session's chat transcript.** While checking whether `NEXTAUTH_SECRET` was still a placeholder, the file was opened with a tool that echoes full file contents — displaying the real `DATABASE_URL` (with password), `ANTHROPIC_API_KEY`, and a short-lived Vercel OIDC token directly in the conversation. Flagged immediately to the user with the option to rotate the Neon password and Anthropic key; **the user chose to leave both as-is** rather than rotate. Recorded here so a future session doesn't need to re-discover this — if credential rotation ever becomes relevant, this is why, and rotation was consciously declined once already. Going forward: read `.env.local`'s specific line via `grep`/`sed` (as already practiced for piping values into `vercel env add`), never via a whole-file read tool.
- **9.6 Deployed to Vercel, team `MAP` (scope `map25`), project `project-brain`**, GitHub-integration-linked to `github.com/paulworrall/Project_Brain`'s `main` branch (auto-connected by `vercel link`) — the repo itself had never been pushed before this checkpoint; pushed `main` for the first time as part of this task, with the user's explicit go-ahead. Production env vars set via `vercel env add` piping straight from `.env.local`/generated values (never displayed): `DATABASE_URL`, `ANTHROPIC_API_KEY`, `NEXTAUTH_SECRET` (the freshly-generated real one, not the placeholder), and `NEXTAUTH_URL` — the last one set only *after* the first deploy, to the stable aliased domain `https://project-brain-ten.vercel.app` (not the per-deployment `-<hash>-map25.vercel.app` URL, which changes every deploy). Redeployed once after setting `NEXTAUTH_URL` so `trustHost`/the auth callback URL matched. Verified live end-to-end: signed in as the seeded PM user, confirmed the taxonomy browser and "Loyalty App Relaunch" project page render real data from the same Neon database dev data has always used (this project has no separate prod database — a deliberate simplification for an internal pilot with one shared environment, not an oversight), and asked the live chatbot a real question that correctly answered from real project documents via a real Anthropic API call.
- **Browser-tool click/keypress dispatch broke mid-session** (confirmed via extensive isolation testing — fresh tabs, server restarts, dev vs. prod mode, coordinate vs. ref-based clicks, mouse vs. keyboard activation — all failed identically; screenshots, navigation, form-filling, and JS evaluation were unaffected throughout). Not an application bug. Verification for the remainder of 9.0 relied on: screenshots for layout/responsive checks, DOM/computed-style inspection via JS for accessibility attributes, and `form.requestSubmit()` via JS (a real, native form submission event, not a fabricated response) to exercise the login and chatbot forms on both local and the live Vercel deployment when the click tool wouldn't cooperate. Flagged to the user mid-session, who chose to proceed on that basis rather than wait.

### Task 7.0 rework — Phase Presentation Layer (2026-08-12)

- **Context**: the PRP and `tasks.md`/`tasks.json` were updated after MVP completion to add a "Stage & Phase Model" section — the flat 10-stage Stage Tracker reads as an engineering decomposition, not how a user thinks about the work. Fix is purely presentational: group Stages into 3 Phases for display, with Stage 10 pulled out as a separate, ongoing "Delivery Monitoring" indicator. Nothing about `Stage`/`ProjectStageStatus` or any agent changed — CLAUDE.md is explicit that this is additive, not a schema change.
- **Regenerating `tasks.md`/`tasks.json` from the updated PRP reset every task's checkbox/status to pending/`[ ]`**, including 1.0-6.0, 8.0, and 9.0, which were all already complete and verified (per this file's own history above, and confirmed by re-running the full test suite/typecheck/lint/build before touching anything). Restored `[x]`/`"completed"` for everything already done, rather than re-doing it — the task list regeneration tool has no knowledge of actual repo state, so its output isn't a re-assessment of progress, just a fresh rendering of the (now Phase-aware) task tree.
- **New `src/lib/phases.ts`**: a plain exported array (`PHASES: Phase[]`, each `{key, name, stageNumbers}`) plus `DELIVERY_MONITORING_STAGE_NUMBER = 10`. No Prisma model, no migration — exactly per CLAUDE.md's instruction.
- **`StageTracker.tsx` rebuilt** from a flat horizontal 10-node strip into 3 Phase cards using native `<details>`/`<summary>` (zero client JS, same zero-JS-disclosure convention already used for Version History) — each phase computes its own aggregate status (`COMPLETE` only if every one of its Stages is `COMPLETE`; `IN_PROGRESS` if any Stage has started; else `NOT_STARTED`) and renders `<details open>` only on the first non-`COMPLETE` phase (or the last phase if all are complete) so the tracker always opens on the phase actually in flight. Expanding a phase reveals its Stage-level detail (same check/number-node styling as the old flat design, unbuilt Stages still marked "(Later)"). A separate, visually distinct "Delivery Monitoring" block renders beneath the phase cards for Stage 10 — dashed border, "Later" badge, deliberately not a 4th phase card.
- **Verified end-to-end in the browser** against the real Neon dev database on "Loyalty App Relaunch" (currently mid Stage 6): confirmed Phase 1 ("Clarifying the brief and scope") shows Complete · 4/4 and renders collapsed, Phase 2 ("Estimation and team planning") shows In progress · 1/3 and renders expanded by default with Stage 5 checked and Stages 6-7 marked "(Later)", Phase 3 ("Statement of work and delivery setup") shows Not started · 0/2 and renders collapsed, and the Delivery Monitoring block renders separately beneath all three with its own "Later" badge — exactly matching the PRP's described UI behavior.
- **`StageTracker.test.tsx` rewritten** for the new structure: phase names render, Stages group under the correct phase (assert via each phase's `<details>` element's `textContent`), the active (non-complete) phase is open by default and the others aren't, a collapsed phase's `<details open>` flips to `true` on a real click of its `<summary>` (via `@testing-library/user-event`, confirming this works without any component-level JS), and unbuilt Stages/Delivery Monitoring still carry their "(Later)"/"Later" labeling. All 5 tests pass; full suite (89 tests, includes other in-progress work already sitting in the tree — see Notes below) + typecheck + lint + production build all clean.
- **Found unrelated, already-in-progress uncommitted work sitting in the working tree** (Client/Workstream drill-down pages under `/clients/[clientId]` and `/workstreams/[workstreamId]`, `GlobalProjectSearch`, `WorkstreamProjectsTable`, plus matching tests) — not part of this task or any checkpoint note in this file, so presumably done in an earlier session and never checkpointed. Left entirely untouched (didn't stage or commit any of it) since it's outside this task's scope and its provenance/intended state isn't recorded anywhere — flagged to the user rather than folding it into this checkpoint's commit.

### Verification pass + committing the Client/Workstream/search feature (2026-08-12)

- **User asked for two things, done independently rather than taken on faith from this file's own prior entries**: (1) an honestly re-verified status report across all 9 parent tasks, and (2) a real review of the uncommitted work flagged above, with a decision to commit or reject it based on its actual state.
- **Verification method, per task** (not just re-reading this file — each item below was actually checked this session):
  - **1.0–2.0**: confirmed `package.json`/`tailwind.config`/`prisma/schema.prisma` exist with the full model set (`Hub`, `Client`, `Workstream`, `Project`, `Stage`, `ProjectStageStatus`, `Document`, `DocumentVersion`, `ChecklistItem`, `TouchpointNote`, `KnowledgeItem`, `User`); confirmed 2 real applied migrations in `prisma/migrations/`; confirmed `prisma/seed.ts` seeds Hub `Caroline` / Clients `Fizzy`/`Coffee`/`Tooth`.
  - **3.0–8.0**: confirmed every agent module (`intake-agent.ts`, `clarification-extraction.ts`, `triage-agent.ts`, `specialist-review-extraction.ts`, `chatbot.ts`) and the parsing utility exist on disk with a matching test file each; confirmed `tests/chatbot-isolation.test.ts` (cross-project isolation) exists and passes.
  - **9.0**: confirmed `tests/stage-1-5-happy-path.test.ts` exists, is not skipped, and passes; grepped the whole `tests/` tree for `.skip`/`.todo`/`xit`/`xdescribe` — none found, so nothing is silently disabled.
  - **All of the above**: ran the *actual* full suite (`npx vitest run` — 22 files, 89 tests, all passing), `npx tsc --noEmit` (clean), and `npm run build` (clean) this session, rather than trusting the historical pass/fail notes above.
  - **9.6 Vercel deployment**: confirmed genuinely real, not just claimed — `.vercel/project.json` links to a real Vercel project (`project-brain`, org `team_zeVVLzRT1S4I9UhjgRrsSbi3`); navigated to the live URL, signed in as the seeded PM user, and confirmed it renders real current data from the real Neon database (Coffee/Fizzy/Tooth clients, "Loyalty App Relaunch" correctly at Stage 6); asked its chatbot a real question and got a real, correctly-grounded answer back (including the referral-feature conflict between the Position/Scope documents and the newer kick-off note — a live Claude API call against live project data, not a canned response).
  - **Live URL**: `https://project-brain-ten.vercel.app`
  - **Important finding**: the live deployment is running the code as of commit `e2e1e90` (the original 9.0 checkpoint) — **neither today's Phase Tracker rework (`ee0cf2a`) nor the Client/Workstream/search commit (`058f7af`) have been pushed to `origin/main`** (`git fetch` + `git rev-list origin/main..HEAD` confirms local `main` is 2 commits ahead). Vercel's GitHub integration auto-deploys on push, so the live site simply hasn't been updated since 9.0 — this is not a deployment failure, just a push that hasn't happened yet. Not pushed as part of this session — pushing to `main` affects a shared/production system and wasn't explicitly requested.
- **Client/Workstream/search feature review**: read every file (`src/app/(dashboard)/actions.ts`'s `searchProjectsAction`, `clients/[clientId]/page.tsx`, `workstreams/[workstreamId]/page.tsx`, `GlobalProjectSearch.tsx`, `WorkstreamProjectsTable.tsx`, plus the breadcrumb/header edits threading them together) — clean, conventional Server-Component-fetches-then-Client-Component-renders code, consistent with the rest of the codebase's patterns (debounced search mirrors nothing pre-existing but follows the same `"use client"` + Server Action call shape as everywhere else; the sortable/filterable table is self-contained client state, no new persistence). Ran its two dedicated test files (9 tests, all passing), then the full suite/typecheck/lint/build again, all clean — **committed as `058f7af`**, a clean, honest commit distinct from today's Phase Tracker checkpoint.

### Push + a genuine deployment bug found and fixed (2026-08-12)

- **User asked to push `main`** (`ee0cf2a` + `058f7af`) — done. The resulting Vercel deployment **errored**: `Module not found: Can't resolve '@/generated/prisma/client'`.
- **Root cause, confirmed via `npx vercel inspect --logs`**: Prisma 7's `prisma-client` generator with a custom output path (`src/generated/prisma`, correctly gitignored) has never had an explicit generate step wired into the Vercel build — no `postinstall` script existed. Every previous production deploy only ever "succeeded" because Vercel's persistent Turbopack build cache kept reusing already-compiled modules resolved from a stale local generate baked into the cache chain all the way back to the very first deployment — a real generate had never actually run on any Vercel build machine. Today's edits happened to touch files that hadn't changed since, forcing a genuine recompile, which is what finally exposed the gap.
- **Fix**: added `"postinstall": "prisma generate"` to `package.json`. Verified by deleting `src/generated/prisma` entirely and re-running `npm install` locally (regenerates cleanly, no `DATABASE_URL` needed since `prisma generate` only reads the schema file), then the full test suite/build again — all clean. Committed as `f022ce3`, pushed on request.
- **Confirmed fixed on the actual live site**: `npx vercel ls` showed the new deployment `● Ready` (34s build); navigated to `https://project-brain-ten.vercel.app`, confirmed the Phase-grouped Stage Tracker, Client/Workstream drill-down, and global search were all live and rendering real data — this is the first deployment that reflects everything built today.

### Stage Tracker duplication fix (2026-08-12)

- **User-reported bug**: after the Phase rework above, each Phase card's expanded area showed a plain numbered-dot list of stage names (no status/content), while a separate section further down the page still rendered the real detailed step cards (status badge, AI Agent/Human Input label, OUTPUT content) for the same stages — every stage's detail effectively existed twice on the page.
- **Fix is a pure composition change, no rewrite of working internals**: `WorkflowStepList.tsx` (the component that already renders the real step cards, expand/collapse, status badges, kind labels, and `step.content`) is untouched. `StageTracker.tsx` now takes `steps: WorkflowStepData[]` (previously the leaner `stages: WorkflowStep[]`, which lacked `content`) and renders `<WorkflowStepList steps={phaseSteps} />` inside each Phase's `<details>` body, replacing the old plain-dot `<ol>`. `ProjectWorkflow.tsx` no longer renders a separate top-level `<WorkflowStepList steps={steps} />` at all — `StageTracker` now occupies the main (`2fr`) column of the grid directly, next to the sidebar, since it's now the single place all step detail lives.
- **Composability turned out to already be correct with zero extra logic**: `WorkflowStepList`'s own "auto-expand the first non-complete step" behavior, combined with `StageTracker`'s "auto-open the first non-complete phase" behavior, compose correctly on their own — e.g. once Phase 1 is fully `COMPLETE` it collapses and Phase 2 (now active) opens with its own first-incomplete step auto-expanded. Confirmed this by re-running `ProjectWorkflow.test.tsx` completely unmodified — all 9 of its existing assertions (which reference specific Steps 3, 4, 5 being visible/expanded under various stage combinations) still passed without a single change, because each assertion's target stage always falls inside whichever phase is active.
- **`StageTracker.test.tsx` rewritten** for the new nested structure: switched its fixture from `WorkflowStep[]` to `WorkflowStepData[]` (adding a `content` node per stage), and added an explicit no-duplication assertion — every phased stage's `"Step N — Name"` text appears in the document exactly once (Stage 10 is correctly excluded from this check, since by design it never gets a step card — only the separate Delivery Monitoring block, which is untouched). Also asserts real status badges ("Complete"/"In Progress") and kind labels ("AI Agent"/"Human Input") now render inside the Phase cards, not a plain numbered dot.
- **Verified visually in the browser** on "Loyalty App Relaunch": Phase 2 ("Estimation and team planning", the active phase) renders open by default showing the real Step 5/6/7 cards including Step 6's live "Coming in task Level 3 (post-MVP)." content and Run Agent button; manually forced Phase 1 open via a real DOM click on its `<summary>` and confirmed Step 1's full real content (INPUT/OUTPUTS, Position Document) renders inside the Phase card, not a plain dot; confirmed via `get_page_text` that no second copy of any step's detail exists anywhere else on the page.
- Full suite grew from 89 to 90 (one net new StageTracker test); typecheck, lint, and production build all clean.

### Phase-scoped step numbering (2026-08-12)

- **User request**: label steps within each Phase relative to that Phase (1.1, 1.2, 1.3... for "Clarifying the brief and scope", 2.1, 2.2... for "Estimation and team planning", 3.1, 3.2... for "Statement of work and delivery setup") instead of the flat 1-10 stage number, to better match the user's mental model of "which phase, how far through it."
- **New `getStepLabel(stageNumber)` in `src/lib/phases.ts`**: the single source of truth for the P.N mapping — looks up which Phase a stage belongs to and its 1-based position within that Phase's `stageNumbers` array, returning e.g. `"1.3"`. Falls back to the plain stage number for a stage not in any Phase (Stage 10 — never hit in practice, since Stage 10 never gets a step card, but kept safe/general rather than assuming). Deliberately a function derived from `PHASES`, not a second hardcoded table, so the two can never drift apart.
- **`WorkflowStepList.tsx`**: added an optional `label?: string` field to `WorkflowStepData` — the header text and the small status-icon circle both now render `step.label ?? String(step.stageNumber)`. Optional and defaulting to the old plain-number behavior means the component itself stays generic/reusable and its own existing test (`WorkflowStepList.test.tsx`, which doesn't set `label`) needed zero changes.
- **`StageTracker.tsx`**: computes `label: getStepLabel(step.stageNumber)` for each step before handing it to the nested `WorkflowStepList` per Phase — this is the only caller that sets `label`, since it's the only place with Phase context.
- **Found and fixed one real inconsistency this surfaced**: `ProjectWorkflow.tsx`'s Triage step had a hardcoded literal `"Waiting on Step 3 to complete."` — now `` `Waiting on Step ${getStepLabel(3)} to complete.` `` (renders "Waiting on Step 1.3 to complete."), so this message can't drift out of sync with the header label a user actually sees for that same stage.
- **Verified in the browser** on "Loyalty App Relaunch": Phase 2's cards read "Step 2.1 — Review with Specialist Leads", "Step 2.2 — Estimation Kick Off" (with the small status circle also showing "2.2", not "6"), "Step 2.3 — Estimation Session"; manually opened Phase 1 and confirmed "Step 1.1 — Intake".
- **Tests updated**: `StageTracker.test.tsx`'s grouping assertions switched from flat numbers to Phase-scoped labels, plus a new dedicated test asserting the status-icon circles show "1.3"/"2.2" and that the old bare flat numbers ("6", "7", "9") no longer appear anywhere (deliberately not asserting bare "1"/"2"/"3" are absent, since the Phase-header badges themselves legitimately render those). `ProjectWorkflow.test.tsx`'s one affected assertion updated to match the new "Waiting on Step 1.3..." text. Full suite grew from 90 to 91; typecheck, lint, and production build all clean.

### Hub page — Workstreams surfaced directly in Client cards (2026-08-12)

- **User request**: reduce navigation depth on the Hub page ("All Projects") — a Client card only ever showed a count ("1 workstream · 1 project"), forcing a click through to the Client detail page just to see Workstream names before reaching Projects. Collapse this for the common case by listing each Client's Workstreams by name directly on the card, each linking straight to that Workstream's project list.
- **Extracted the card markup into its own component, `src/components/features/ClientWorkstreamCard.tsx`**, rather than inlining it in the page — same pattern already used for `WorkstreamProjectsTable`: the Server Component page (`src/app/(dashboard)/page.tsx`) does the Prisma fetch and maps to plain serializable props (`{id, name, workstreams: [{id, name, projectCount}]}`), the extracted component is pure presentation and independently testable. `TaxonomyBrowserPage` itself still isn't unit-tested (this codebase's established convention for Server Components that call Prisma directly), but now the part that actually needed coverage is.
- **Card structure changed from "whole card is one `<Link>` to `/clients/[id]`" to "Client name is its own link, each Workstream name is its own separate link"** — nesting an `<a>` inside another `<a>` (the old single-link-wrapped-card wouldn't have supported per-Workstream links) is invalid HTML and a real Next.js hydration hazard, not just a style choice. The Client name link to `/clients/[clientId]` is kept exactly as before — the existing Client detail page is untouched, this is additive.
- **Query change**: dropped the separate `_count: { select: { workstreams: true } }` aggregate (no longer needed, since Workstreams are now listed individually) and now selects each Workstream's own `id`/`name`/`_count.projects` instead of just an aggregate project count summed across all of a Client's Workstreams.
- **Handles zero/one/many Workstreams generically, not just today's 1-per-Client seed data**: an empty list renders "No workstreams yet"; the `<ul>`/`<li>` structure and Tailwind classes make no assumption about count (no fixed-width columns, no hardcoded nth-child styling) — verified explicitly with a 3-Workstream fixture in the component test, since real seed data doesn't have that case to check against in the browser.
- **`tests/components/ClientWorkstreamCard.test.tsx`** (5 tests, new): Client name links to `/clients/[id]`; empty state shows "No workstreams yet"; a single Workstream renders name + "· 1 project" (singular) linking to `/workstreams/[id]`; zero-project pluralization ("· 0 projects"); and the multi-Workstream case explicitly required by the task (3 Workstreams, each with a distinct project count, each linking to its own correct `/workstreams/[id]` — the case seed data can't currently exercise).
- **Verified visually in the browser against real seeded data** (not just the component test): "Coffee" → "Coffee Loyalty App · 1 project", "Fizzy" → "Fizzy Refresh 2026 · 2 projects", "Tooth" → "Tooth Rebrand · 0 projects"; clicked a Workstream link and confirmed it landed directly on that Workstream's project table (skipping the Client page entirely); separately navigated to a Client's own link and confirmed `/clients/[clientId]` still renders correctly, unchanged. Checked at mobile width (375px) via `document.documentElement.scrollWidth` — no horizontal overflow introduced (screenshot capture was unavailable in this session's Browser pane, same known tool-side quirk as previous sessions — verified via DOM measurement instead).
- Full suite grew from 91 to 96 (5 new `ClientWorkstreamCard` tests); typecheck, lint, and production build all clean.

### Client-level commercial documents: MSAs, Rate Cards, first real role enforcement (2026-08-13)

- **User request**: Master Service Agreements and Rate Cards, scoped to Client (not Project), with write access restricted to the ClientEngagement role, and a Rate Card selectable at Project creation/edit — scoped strictly to the chosen Project's own Client.
- **Additive schema only**, exactly as specified: `MasterServiceAgreement` (`clientId`, `fileName`, `fileBytes` (Postgres `bytea`, Prisma `Bytes`), `extractedText`, `effectiveFrom`/`effectiveTo`, `status: ACTIVE|SUPERSEDED`, `uploadedBy`/`uploadedAt`) and `RateCard` (same shape plus `name`/`currency`, `status: ACTIVE|ARCHIVED`), both with `onDelete: Cascade` from Client and `onDelete: SetNull` from the uploading User. `Project.rateCardId` added as a nullable FK (`onDelete: SetNull`). Migration `20260813080802_add_commercial_documents` applied via the documented WSL workaround — worked first try. No existing model touched.
- **First real role enforcement in the app.** CLAUDE.md's Authentication Flow section previously said "both roles see the same views — do not build restrictive permissions yet"; this task is the first exception, and deliberately narrow: `src/lib/permissions.ts` exports `isClientEngagement(session)` + a shared rejection message, used by every write action below. **The check lives in the Server Action itself, not the UI** — a `canManage` prop hides the buttons as a UX nicety only; tests prove the server rejects the write even when a component would never have rendered the button in the first place.
- **New `src/app/(dashboard)/clients/[clientId]/actions.ts`**: `createMasterServiceAgreementAction` (role-checked; in a transaction, marks the Client's current Active MSA `SUPERSEDED` before inserting the new one as `ACTIVE` — "Add MSA" and "Replace MSA" are the same action, per the front-end spec), `createRateCardAction` (role-checked; RateCards don't auto-supersede each other — a Client can have several concurrently Active, named ones, e.g. "Retainer Rates" vs "Project Rates"), `archiveRateCardAction` (role-checked; scoped by both `id` and `clientId` in the same defense-in-depth `updateMany` pattern as `toggleChecklistItemAction`). Both create actions reuse `parseDocumentToText` (task 4.2) on the uploaded file for `extractedText`, storing the raw bytes too (`Buffer.from(await file.arrayBuffer())`) — the first model in this codebase to persist raw file bytes rather than parsed text alone.
- **Client-scoped Rate Card selection — the actual isolation boundary, not just hidden options**:
  - **At Project creation**: `getRateCardsForWorkstreamAction(workstreamId)` (new, in `projects/new/actions.ts`) resolves the Workstream's `clientId` first, then queries `RateCard` filtered to only that `clientId` — a different Client's Rate Cards are never fetched into the page, matching the exact pattern CLAUDE.md already mandates for the chatbot's `project_id` scoping. `NewProjectForm.tsx` calls this action on every Workstream-select change (debounce-free `useTransition`, same shape as `GlobalProjectSearch`'s pattern) and repopulates the Rate Card `<select>`, disabled with a contextual hint ("Select a workstream first" / "No rate cards for this client") until real options exist.
  - **At Project edit** (`ProjectSummaryBar`): the Project's Client is already fixed and known server-side, so `projects/[projectId]/page.tsx` just queries that exact Client's Active Rate Cards directly — no dynamic action needed, no ambiguity to guard against.
  - **Both write paths re-validate server-side regardless of what the dropdown showed**: `createProjectAction` and `updateProjectSummaryAction` both look up the target Client itself and confirm the submitted `rateCardId` actually belongs to it before persisting — never trusting that the submitted id matches what the (already-scoped) dropdown offered.
  - **Real bug caught here, not just a test artifact**: a `<select disabled>` is omitted from `FormData` entirely on submit (native HTML behavior) — `formData.get("rateCardId")` returns `null`, not `undefined`, whenever no Rate Card options exist yet. `z.string().optional()` only accepts `undefined` and rejects `null`, so the very first real submission of "New Project" with no Rate Card chosen would have failed outright. Fixed by switching `CreateProjectSchema`'s `rateCardId` to `.nullish()`. Caught by the pre-existing `stage-1-5-happy-path.test.ts` failing after this feature landed — a good example of why that test exists.
- **Front end**: `MasterServiceAgreementsPanel.tsx` and `RateCardsPanel.tsx` (new, both presentational-plus-form Client Components mirroring `KnowledgeUpload`'s list+form-toggle shape), added to a new "Contracts & Rates" section on `/clients/[clientId]`. `ProjectSummaryBar` gained a `Rate Card` view field + edit-mode `<select>`, `NewProjectForm` gained the dynamic Rate Card `<select>` described above.
- **Tests**: `tests/commercial-documents-permissions.test.ts` (7, real-DB, real throwaway CLIENT_ENGAGEMENT/DELIVERY Users under a throwaway Hub/Client, `@/lib/auth` mocked per-test to switch role) — proves a ClientEngagement user can upload the first MSA, replace it (superseding the old one), create a Rate Card, and archive it; and proves a Delivery user attempting each of those is rejected server-side with **zero row created/changed**, not just shown an error. `tests/components/MasterServiceAgreementsPanel.test.tsx` (6) and `RateCardsPanel.test.tsx` (5) cover list rendering, the `canManage` UI-gating nicety, and the Active-vs-Superseded/Archived button visibility rules. `tests/components/NewProjectForm.test.tsx` (4, new — this component had no prior test coverage) covers the dynamic client-scoped fetch, the disabled/hint states, and that switching Workstreams clears the previous Client's options rather than leaving them stale. `ProjectSummaryBar.test.tsx` updated for the new prop shape and Rate Card field.
- **Verified end-to-end in the browser against the real Neon database, as both roles**: as ClientEngagement (`am@projectbrain.test`), uploaded a real MSA and a real Rate Card to the Coffee Client (files attached via a `DataTransfer`-based `javascript_tool` script, since this session's Browser-pane tooling has no native file-picker support) — confirmed "Add MSA" flips to "Replace MSA" once Active, status badges render correctly. Switched to Delivery (`pm@projectbrain.test`): confirmed the Contracts & Rates lists render fully read-only (no Add/Replace/Archive buttons anywhere), then confirmed Delivery **can** still select Coffee's real Rate Card both on `/projects/new` (after picking the Coffee Workstream) and on an existing Coffee project's edit form — saved it, confirmed "RATE CARD" showed the real value, then reverted it back to "Not yet set" to leave demo data clean. Separately confirmed, as ClientEngagement, that selecting the Fizzy Workstream on `/projects/new` shows "No rate cards for this client" with Coffee's Rate Card nowhere in the option list — the actual cross-Client isolation the task required.
- Full suite grew from 96 to 118 (7 permissions + 6 `MasterServiceAgreementsPanel` + 5 `RateCardsPanel` + 4 `NewProjectForm`, new; `ProjectSummaryBar.test.tsx` updated in place for the new prop shape, still 4 tests); typecheck, lint, and production build all clean.

### Taxonomy UX redesign: Client/Workstream drill-down + global search (2026-08-14)

- **User request**: the flat "All Projects" page didn't scale to 6-8 Workstreams with tens of Projects each — establish a firmer Hub → Client → Workstream → Project taxonomy in the UI, not just the data model.
- Landing page (`/`) rebuilt as Client cards showing workstream/project counts; new `/clients/[clientId]` and `/workstreams/[workstreamId]` detail pages, the latter backed by `WorkstreamProjectsTable` (client-side sortable/filterable table — search, stage filter, click-to-sort columns).
- New debounced global project search (`GlobalProjectSearch`) in the Header, backed by `searchProjectsAction` (case-insensitive `contains`, 2-char minimum, capped at 8 results).
- Breadcrumbs on the Project Detail/Outputs/Version History pages upgraded from plain text to real links back up the taxonomy.
- Found and fixed a real mobile layout bug via actual browser testing (not assumed): the header's flex row didn't shrink the search box properly at 375px, squeezing "Project Brain" onto two lines — fixed with `shrink-0`/`min-w-0 flex-1`/`hidden sm:block` adjustments to `Header.tsx` and `GlobalProjectSearch.tsx`.
- New tests: `WorkstreamProjectsTable.test.tsx` (6), `GlobalProjectSearch.test.tsx` (3), `ClientWorkstreamCard.test.tsx` was already covering the Hub-page card. Verified live against real seeded data (Fizzy → Fizzy Refresh 2026 → table showing both real projects, sort/search/filter all working) at mobile, tablet, and desktop widths.

### Phase 1 rework: from a 4-step gated sequence to a fluid workspace (2026-08-17)

- **User request**: Phase 1 ("Clarifying the brief and scope") previously showed 4 numbered steps (Intake / Clarification Email Sent / Get Clarifications / Triage) each with its own Pending/In Progress/Complete badge — implying a rigid linear gate. Real clarification happens repeatedly, over days or weeks, outside the platform, so this needed to become a living workspace instead of a one-shot sequence.
- **Additive schema only**: `ChecklistItem.detailText String?` — a freeform field (job code, folder URL, etc.) always editable regardless of `isComplete`. `TouchpointNote` already supported many-per-project with no unique constraint, so no schema change was needed there — the single-note assumption lived entirely in the UI/query layer (`.find()` picking just the most recent one), not the schema.
- **Backend — two Server Actions renamed and made genuinely repeatable** (in `src/app/(dashboard)/projects/[projectId]/actions.ts`):
  - `submitClarificationNotesAction` → `submitClientUpdateAction`: no longer touches `ProjectStageStatus` at all — it just runs the existing extraction, adds a new Position Document version, and appends a `TouchpointNote`. Callable any number of times.
  - `runTriageAgentAction` → `generateDraftScopeDocumentAction`: on **first** run only, transitions Stage 3/4 to `COMPLETE` and Stage 5 to `IN_PROGRESS` (the same handoff `runTriageAgentAction` used to do once); on every **subsequent** run, just appends a new `DocumentVersion` without re-touching stage status, so regenerating after specialist review has already started doesn't regress Phase 2 back out of progress. Existence-check happens *inside* the transaction (`tx.document.findUnique`), not before it, to avoid a race.
  - New `updateChecklistItemDetailAction` — scoped by `id`+`projectId` like `toggleChecklistItemAction`, deliberately a separate action so the checkbox and the detail text can never interfere with each other.
  - `createProjectAction` (in `projects/new/actions.ts`) now marks Stage 2 (Clarification Email Sent) `COMPLETE` immediately alongside Stage 1 — the email is generated as part of the same Intake Agent run and is no longer a separate pipeline step with its own status (per the front-end spec: "exposed as a downloadable/viewable file, not a separate step"). Stage 3 (Get Clarifications) opens straight away as `IN_PROGRESS`; `currentStageNumber` starts at 3, not 2.
- **Real bug caught via live browser verification, not just tests**: the seeded "Lemonade project" predates this change and only had Stage 1/2 `ProjectStageStatus` rows (Stage 3's row used to only get created once a clarification reply was submitted). `generateDraftScopeDocumentAction`'s first-run transition used `tx.projectStageStatus.update()` for Stage 3, which throws Prisma `P2025` ("no record found") when that row doesn't already exist — exactly what happened clicking "Generate" on that real project in the browser. Fixed by switching Stage 3 to `upsert`, matching Stage 4/5's existing pattern. Added a regression test (`phase1-workspace.test.ts`) covering a project with zero pre-existing `ProjectStageStatus` rows, specifically because this class of bug (new code assuming old code's side effects already ran) doesn't show up in tests that only exercise the new code path end-to-end from a fresh fixture.
- **Front end**: `StageTracker.tsx` takes a new `phase1Status: "NOT_STARTED" | "IN_PROGRESS" | "READY_FOR_SPECIALIST_REVIEW"` + `phase1Content: ReactNode` pair instead of deriving Phase 1's badge/content from `steps` the way Phase 2/3 still do — Phase 1's accordion header now reads "Not started"/"In progress"/"Ready for specialist review" instead of "x/4 stages". `READY_FOR_SPECIALIST_REVIEW` is a status flag only, describing what Phase 1 IS, not yet a real handoff mechanism — the actual Phase 1 → Phase 2 UX (does Phase 1 lock, does a PM explicitly "send" it forward, etc.) is intentionally deferred to when Phase 2 itself gets reviewed next.
  - New `Phase1Workspace.tsx` composes: the live Position Document (`PositionDocumentView`, reused as-is) under a "Current position" heading; `ClientUpdateComposer.tsx` (textarea + submit, keyed on the log's length so a successful submission remounts/clears the form while a failed one preserves what was typed, plus the full timestamped log below it); `ClarificationEmailCard.tsx` and `DraftScopeDocumentCard.tsx` side by side (the former adds a client-side `Blob`/`<a download>` Download button — no server round-trip; the latter shows "Not yet generated"/Generate or the current version + Regenerate); `EditableChecklist` (existing component, extended) underneath.
  - `EditableChecklist.tsx`: each item now renders a checkbox (unchanged) plus an always-editable text `<input>` for `detailText`, submitted via its own action (`updateChecklistItemDetailAction`) on blur. The cross-instance-sync key (this checklist still renders in two places — the workspace and the sidebar) extended from `` `${id}-${isComplete}` `` to `` `${id}-${isComplete}-${detailText ?? ""}` ``.
  - Deleted `ClarificationNotesForm.tsx` and `RunTriageAgentButton.tsx` — fully superseded, no remaining references.
- **Verified end-to-end in the browser against the real Neon database and real Claude API**, on the seeded "Lemonade project": submitted a real client update ("...summer refresh campaign... 18-34 year olds... budget of £50k") and watched the Position Document's "What We Know"/"What We Need to Find Out" update live and the update appear in the timestamped log with the real signed-in PM's name; generated the Draft Scope Document for the first time (hit and fixed the P2025 bug above along the way) and confirmed "Regenerate" + "Version 1 — [timestamp]" + the full generated content with flagged gaps render correctly; confirmed the Phase 1 → Phase 2 handoff actually works end-to-end — Phase 1's badge flipped to "✓ Ready for specialist review" and collapsed, while Phase 2 auto-expanded showing Stage 5's "Review with Specialist Leads" form `IN_PROGRESS`, exactly as Phase 2 already behaved before this change.
- **Tests**: `tests/phase1-workspace.test.ts` (new, real-DB integration, 4 tests) — multiple sequential client updates each produce a new Position Document version + log entry; Draft Scope Document generation can be triggered multiple times without regressing Stage 5; checklist `detailText` persists independently of `isComplete` through toggles in both directions; the legacy-project regression test above. `StageTracker.test.tsx` and `ProjectWorkflow.test.tsx` rewritten for the new Phase 1 props/content (Phase 2/3 assertions otherwise unchanged, explicitly proving they're unaffected). New component tests: `ClientUpdateComposer.test.tsx`, `ClarificationEmailCard.test.tsx` (including the download Blob behavior), `DraftScopeDocumentCard.test.tsx`, `Phase1Workspace.test.tsx`. `EditableChecklist.test.tsx` extended for the detail field (and picked up a latent mock-isolation bug of its own — `toggleChecklistItemAction`'s call count wasn't being reset between tests in that file, which only mattered once a test needed to assert it was *not* called).
- Full suite is now 140 tests, all passing (new: `tests/phase1-workspace.test.ts` (4), `tests/components/ClientUpdateComposer.test.tsx` (4), `tests/components/ClarificationEmailCard.test.tsx` (4), `tests/components/DraftScopeDocumentCard.test.tsx` (5), `tests/components/Phase1Workspace.test.tsx` (5); `EditableChecklist.test.tsx` grew from 5 to 7; `StageTracker.test.tsx` and `ProjectWorkflow.test.tsx` rewritten in place); typecheck, lint, and production build all clean. Not yet pushed — committed locally pending user review, per this session's instruction.

### Phase 1 workspace: progressive disclosure pass (2026-08-17)

- **User request**: the just-shipped Phase 1 fluid workspace was too long and had a real duplication bug — the Project Set-Up Checklist rendered twice (main column + sidebar). Reduce the page to a scannable, progressively-disclosed layout using the codebase's existing `<details>` pattern (already used for the Phase cards and Version History) rather than inventing a new interaction style.
- **Bug fix**: `Phase1Workspace.tsx` was rendering its own `<EditableChecklist>` in addition to `ProjectWorkflow.tsx`'s sidebar instance — a real duplication, not by design (unlike the old step-based layout, where a checklist appearing on both "Step 1" and the sidebar was an intentional, explicitly-tested two-copies-in-sync pattern; that pattern doesn't apply here since Phase 1 no longer has a "Step 1"). Removed the workspace's own instance entirely — the sidebar is now the single source. `ProjectWorkflow.test.tsx`'s dedup test now asserts exactly 1 checklist, not 2.
- **New compact progress strip** at the top of the workspace (`Phase1Workspace.tsx`): "`X confirmed detail(s) · Y open question(s) · Z client update(s) logged · N/M checklist items complete`", computed live from `positionDocument.whatWeKnow`/`whatWeNeedToFindOut`, `clientUpdates.length`, and `checklistItems` — denominator is the real item count, not a hardcoded 5, since a project's checklist size isn't guaranteed.
- **Truncate-and-expand for long lists** (`PositionDocumentView.tsx`): a new `TruncatedList` helper shows the first 5 items with a `<details>`/`<summary>` "Show N more" toggle for lists over 6 items — applied only to "What We Need to Find Out" (the list expected to grow long as gaps accumulate). "What We Know" and "Client-Flagged Open Items" are deliberately left untouched at any length, per the spec — they're the primary focus of this redesign, not something to bury.
- **Collapsed update history** (`ClientUpdateComposer.tsx`): "Previous updates" now always shows only the single most recent entry; a `<details>` "View all updates (N)" toggle reveals the rest (`updates.slice(1)`) — N is the total count. Relies on `clientUpdates` arriving newest-first, which is how the project page already queries `TouchpointNote`s.
- **Compact summary cards replace full inline documents — the single biggest length reduction**: `ClarificationEmailCard.tsx` and `DraftScopeDocumentCard.tsx` no longer render `ClarificationEmailView`/`DraftScopeDocumentView` at all. Each now shows a title/version line, one computed stat (email: word count; scope doc: "6 sections · N gaps flagged", 6 being fixed by `DraftScopeDocumentSchema`'s shape), the existing Download/Generate/Regenerate action, and a "View full email/draft →" link. **The Outputs Library / Version History pages (`/projects/[id]/outputs/[documentType]`) are the single canonical place these render in full** — no second full-content rendering path was added; that page already supported linking directly to one document type, so no changes were needed there. The "⚠ Gaps Carried Forward for Specialists" warning moved with the rest of the Draft Scope Document's content — it now only ever appears at that canonical Version History link, not duplicated on the Phase 1 page.
- **Verified end-to-end in the browser** against the real "Lemonade project" (now carrying 2 real client updates and a 17-gap Draft Scope Document from prior live testing — a good stress case for the new collapsing behavior): progress strip showed "5 confirmed details · 10 open questions · 2 client updates logged · 0/5 checklist items complete"; "What We Need to Find Out" showed 5 of 10 items plus "Show 5 more", which correctly revealed the remaining 5 on click; "Previous updates" showed only the latest of 2 entries behind a "View all updates (2)" toggle; both document cards showed compact summaries only (342 words / 6 sections · 17 gaps flagged) with working "View full →" links that land on the Outputs Library page showing the complete content, gaps warning included; confirmed the checklist now renders exactly once (sidebar only).
- **Tests**: new `tests/components/PositionDocumentView.test.tsx` (5) covering the truncation threshold, the "Show N more" toggle, and that "What We Know"/"Client-Flagged Open Items" never truncate. `ClientUpdateComposer.test.tsx`, `ClarificationEmailCard.test.tsx`, and `DraftScopeDocumentCard.test.tsx` rewritten for the collapsed/compact behavior (including asserting the full content and the old "not called" duplication no longer appear). `Phase1Workspace.test.tsx` gained progress-strip assertions and an explicit "does not render the checklist" test. `ProjectWorkflow.test.tsx`'s checklist test renamed and now asserts exactly one instance. One test-writing note: `<details>` content stays mounted in the DOM even when collapsed, so asserting "not visible" requires jest-dom's `.not.toBeVisible()` (which understands closed `<details>`), not `.not.toBeInTheDocument()` (which only checks presence, not visibility) — caught by a real test failure while writing the collapse test, not assumed.
- Full suite grew from 140 to 149 tests, all passing; typecheck, lint, and production build all clean. Not yet pushed — committed locally pending user review, per this session's instruction.

### Commercial documents generalized: SOW Template Library + shared document/version/revert pattern (2026-08-18)

- **User request**: generalize commercial document management across the platform — a new SOW Template Library, and upgrade MSA and Rate Cards to share the same versioning/revert pattern, so all three commercial document types behave consistently. Named document (e.g. a specific Rate Card, the MSA, a specific SOW Template) has many Versions; exactly one is current (`ENABLED`); uploading a new version disables the previous one; reverting re-enables an old one and disables the current one; nothing is ever deleted; PMs only ever see the current version, disabled versions stay visible in an admin history view.
- **Schema — new models, and MSA/RateCard split into document+version pairs** (`prisma/schema.prisma`): new shared `VersionStatus` enum (`ENABLED`/`DISABLED`) used by all three version tables. `MasterServiceAgreement` is now the "document" (just `id`/`clientId` — `@@unique([clientId])`, since there's only ever one MSA per Client, unlike Rate Cards) with a new `MasterServiceAgreementVersion` child holding the file/dates/status/uploader. `RateCard` similarly split into the document (`id`/`clientId`/`name`/`currency`) and a new `RateCardVersion` child. New `SOWTemplate` (`id`/`name`/`scope: GLOBAL|CLIENT_SPECIFIC`/`clientId` nullable/`isBaseline`) + `SOWTemplateVersion`. `Project.sowTemplateId` added (nullable FK, `onDelete: SetNull`) — only records the PM's selection; the SOW-generation agent that would consume it is future/Level 3 scope, not built here.
- **Hand-authored data migration (`prisma/migrations/20260818140000_generalize_commercial_documents/migration.sql`), not machine-generated**: Prisma's own `migrate dev` diff would have just dropped the file/status columns being moved onto the new Version tables, losing every existing MSA/Rate Card. Instead: rename the old tables out of the way (`_old_MasterServiceAgreement`/`_old_RateCard`, including their now-colliding `_pkey` constraint names), create the new document+version tables, copy every old row forward, then drop the old tables. MSA rows (which could have several per Client under the old ACTIVE/SUPERSEDED replace-history model) are consolidated into **one** document per Client with each old row becoming a Version underneath it (oldest = version 1, ACTIVE→ENABLED, SUPERSEDED→DISABLED) — genuine history preservation, not a naive 1:1 copy. Rate Card rows map 1:1 onto new documents, **reusing the same id**, so every existing `Project.rateCardId` reference stays valid with zero rewrite needed. Applied via `prisma migrate deploy` (non-interactive; `migrate dev` refuses to run in this environment once it detects a data-loss-shaped diff). Verified directly against the real dev DB afterward: the real Coffee Client's MSA and Fizzy's Rate Card both survived with 1 version each, correctly `ENABLED`.
- **One reusable UI component** (`src/components/features/VersionHistory.tsx`) used identically by MSA, Rate Cards, and SOW Templates — not three separate implementations. Shows the current version prominently; older (disabled) versions collapse behind a `<details>` "Version history (N)" toggle (progressive-disclosure pattern, consistent with the Phase 1 work) with a "Revert to this version" action on each; an "Upload"/"Upload new version" toggle reveals a form (file input + an optional `children` slot for document-type-specific extra fields, e.g. MSA/Rate Card's effective-date range — SOW Templates need none).
- **Backend — repeatable versioned actions, ClientEngagement-only, enforced server-side** (`src/app/(dashboard)/clients/[clientId]/actions.ts`, new `src/app/(dashboard)/sow-templates/actions.ts`): `uploadMasterServiceAgreementVersionAction` (upserts the Client's MSA document on first upload, disables the current version, creates the new one), `revertMasterServiceAgreementVersionAction`; `createRateCardAction` (new named document + first version), `uploadRateCardVersionAction`, `revertRateCardVersionAction`; `createClientSpecificSOWTemplateAction`, `uploadSOWTemplateVersionAction`, `revertSOWTemplateVersionAction`. All revert/upload actions are scoped by both the version id and its parent document id (or Client id), the same defense-in-depth convention already used elsewhere in this codebase. Extracted the repeated file-parsing error-handling wrapper into `src/lib/extractTextFromBuffer.ts` (previously duplicated inline in the Client actions file; now shared by both actions files). **Dropped `archiveRateCardAction`** — the old whole-document "archive" concept doesn't map onto the new model (there's no document-level status anymore, only per-version); deprecating old content is now a version-level concern (upload a new version, or simply don't revert back to an old one), which wasn't asked for as a separate feature to preserve.
- **`getSOWTemplatesForClientAction(clientId)`** — the isolation boundary: `WHERE clientId = X OR clientId IS NULL` (baseline), mirroring the existing `getRateCardsForWorkstreamAction` pattern. A different Client's variant is never fetched, not just hidden.
- **`startSowDevelopmentAction(projectId, ...)`** (`projects/[projectId]/actions.ts`) — sets `Project.sowTemplateId`, re-validating server-side that the submitted template is the baseline or belongs to the Project's own Client. **No role check** — per the spec, Delivery can select a current Rate Card or SOW Template (a Project field write), only writes to the documents themselves are ClientEngagement-only.
- **Front end**: `MasterServiceAgreementsPanel.tsx` and `RateCardsPanel.tsx` rewritten to wrap `VersionHistory` (Rate Cards render one `VersionHistory` per named document, plus a separate "Add rate card" create form — a new named document isn't a "new version" of anything). New `/sow-templates` library page (Server Component) — baseline `VersionHistory` at the top, client-specific variants grouped by Client below — added to the Sidebar nav. New `ClientSowTemplatesSection.tsx` on the Client detail page — a **read-only** view (baseline + this Client's variants, current version's filename only, no upload/revert controls) plus a quick "Add [Client]-specific variant" create action; full version management for every template lives only in the library page, avoiding a second implementation of that logic. New `StartSowDevelopmentPanel.tsx` wired into Stage 8 ("Commercials & SOW", Phase 3) in `ProjectWorkflow.tsx` — a template `<select>` + save button, with "Generate SOW" kept as an explicitly disabled placeholder.
- **Real bug caught during browser verification, not just tests**: `src/app/(dashboard)/sow-templates/page.tsx` (a Server Component) passed `makeRevertAction={(versionId) => action.bind(null, id, versionId)}` — a **plain closure**, not a Server Action nor a direct `.bind()` of one — directly as a prop into the Client Component `VersionHistory`. Next.js only allows an actual Server Action reference (or a direct bind of one) to cross the Server→Client boundary as a prop; a new arbitrary closure wrapping one is rejected at render time ("Functions cannot be passed directly to Client Components..."), which only surfaces by actually loading the page, not at build or typecheck time. Fixed by adding a thin Client Component wrapper (`SOWTemplateVersionHistory.tsx`) that builds the `onUpload`/`makeRevertAction` closures internally (entirely client-side, no boundary crossing) — the same shape `MasterServiceAgreementsPanel`/`RateCardsPanel` already used correctly, since those are Client Components themselves. Confirms the general rule for this codebase: any `VersionHistory` usage from a Server Component needs this wrapper pattern; usages from an already-`"use client"` component (like the two commercial-document panels) can build the closures inline.
- **Verified end-to-end in the browser as ClientEngagement** (`am@projectbrain.test`): uploaded a real baseline SOW Template version from the library page (confirmed "Current" + "Version 1" render correctly); added a real Fizzy-specific variant from the Fizzy Client page (confirmed it appears in both the Client page's read view and the library page, grouped under "Fizzy"); on a real Fizzy project's Stage 8 panel, confirmed the SOW Template `<select>` offered exactly "Standard SOW Template (baseline)" and "Fizzy Retainer SOW"; selected the variant and confirmed "Using Fizzy Retainer SOW." persisted after a page reload; **confirmed the isolation guarantee directly** — the Coffee Client's own project's Stage 8 selector showed only the baseline, with Fizzy's variant nowhere in the option list, not just hidden.
- **Tests**: `tests/commercial-documents-versioning.test.ts` (new, real-DB, 10 tests) — a database-wide migration-integrity check (no document of any of the three types ever has more than one `ENABLED` version, covering both this session's newly-created data and whatever survived the migration), upload-disables-previous and revert-re-enables for MSA and Rate Cards, SOW Template Client-isolation, and `startSowDevelopmentAction`'s role-free-but-still-validated behavior. `tests/commercial-documents-permissions.test.ts` rewritten for the new action names/shapes plus new SOW Template permission cases. New `tests/components/VersionHistory.test.tsx` (8) — the shared component's collapse/expand, current-version display, per-version revert wiring, and the `children`-slot reuse pattern (with vs. without extra fields). `MasterServiceAgreementsPanel.test.tsx` and `RateCardsPanel.test.tsx` rewritten for the new versioned props. New `StartSowDevelopmentPanel.test.tsx` and `ClientSowTemplatesSection.test.tsx`. One test-writing note: a required `<input type="file">` blocks native form submission in jsdom even after `userEvent.upload()` sets its `files` in some scenarios — worked around with `fireEvent.submit(form)` (dispatches the submit event directly, bypassing the constraint-validation gate a real button click goes through) rather than removing the `required` attribute from real product code.
- Full suite grew from 149 to 183 tests, all passing (confirmed on two separate full runs — one unrelated `ChatPanel.test.tsx` flake self-resolved on rerun, not a regression); typecheck, lint, and production build all clean. Not yet pushed — committed locally pending user review, per this session's instruction.

### Extended the SOW Templates "library" UX pattern to MSA and Rate Cards (2026-08-18)

- **User request**: the SOW Templates section on the Client detail page already showed a compact library-style read view (name/tag/filename rows + "Manage in library →" link) rather than full inline management. Extend that exact pattern to the Master Service Agreement and Rate Cards sections, which still showed static "No MSA on file yet."/"No rate cards on file yet." placeholders backed by full inline `VersionHistory` panels.
- **New shared component, generalized rather than duplicated** (`src/components/features/LibrarySummaryList.tsx`): the header + "Manage in library →" link + row list (name, optional `(tag)`, right-aligned filename or "Not yet uploaded") that was previously hand-rolled inside `ClientSowTemplatesSection.tsx`. That component was refactored to build its rows and pass them into `LibrarySummaryList`, keeping only its own SOW-specific "Add [Client]-specific variant" quick-add form as `children`. Rows list at `max-h-64 overflow-y-auto` so a long list scrolls inside its own card rather than growing the page.
- **Two new library pages**, mirroring `/sow-templates`'s existing structure exactly: `/master-service-agreements` and `/rate-cards`, each listing every Client (grouped, like SOW's client-specific variants) with the existing `MasterServiceAgreementsPanel`/`RateCardsPanel` (unchanged `VersionHistory`-based full management) rendered per Client — including Clients with zero documents yet, so the very first upload for a new Client still has a path. Both added to `Sidebar.tsx`'s nav alongside "SOW Templates".
- **Creation moved off `RateCardsPanel`, onto the Client page — matching exactly where SOW variant creation already lived**: the old inline "Add rate card" name/currency/file/dates form was stripped out of `RateCardsPanel.tsx` (now purely a per-Client list of `VersionHistory` panels for Rate Cards that already exist, used only on the library page) and rebuilt in a new `ClientRateCardsSummary.tsx` (the Client page's read view + quick-add), reusing the exported `EffectiveDateFields` helper from `RateCardsPanel.tsx` rather than duplicating the two date inputs a second time. New `ClientMasterServiceAgreementSummary.tsx` has no quick-add at all — an MSA has no "create a new named document" step the way Rate Cards/SOW variants do (it's a single document per Client, auto-created on first upload), so there's nothing analogous to offer here; only the read row + link out.
- **Tag conventions, per type**: MSA's current version tagged "active"; a Rate Card's current version tagged "current"; SOW's baseline tagged "baseline" (unchanged) — deliberately different words per type since they describe different things (an MSA's *and* Rate Card's currency both being "the current one" reads naturally as "active"/"current" respectively; "baseline" describes the *global* template's role, not its currentness).
- **Accessibility**: added a visible `focus-visible` outline to the "Manage in library" links (all three now share the same ring style already used by `Button`/`Input`) — the original SOW link had none, so this improves rather than merely "matches" the prior standard, per the explicit ask to confirm keyboard/focus accessibility.
- **Caught one real test bug while writing coverage**: an early version of `ClientMasterServiceAgreementSummary.test.tsx` asserted `screen.getByText("Master Service Agreement")`, which is genuinely ambiguous — it matches both the card's `<h3>` title and the list row's name (both say "Master Service Agreement" for this document type specifically, unlike Rate Cards/SOW where the row name differs from the section title). Fixed by scoping to `getByRole("heading", { name: ... })` for the title assertion.
- **One unrelated flaky test found and fixed in passing**: `ChatPanel.test.tsx` asserted the optimistic "You: {question}" line synchronously immediately after the click, before React had flushed that state update — passed in isolation but flaked under the full suite's parallel load. Wrapped in `waitFor`, consistent with the adjacent assertion in the same test. Confirmed via `git log`/diff that `ChatPanel.tsx` itself was untouched this session — this was a pre-existing latent flake, not a regression from this work.
- **Verified in the browser as ClientEngagement** (`am@projectbrain.test`): Fizzy (no MSA/Rate Card yet) correctly shows both empty states side by side with SOW's populated list, proving the empty-vs-populated branch never shows both; Coffee (real migrated data) shows "Master Service Agreement (active)" → `MSA_Coffee_2026.txt` and "2026 Standard Rates (GBP) (current)" → `RateCard_2026.txt` as proper library rows; all three "Manage in library" links resolve to their correct, distinct destination pages (confirmed by reading each link's actual `href`, not just its label) and each library page renders every Client (including empty ones, with a working first-upload path) with the pre-existing `VersionHistory` controls intact; keyboard-focused a "Manage in library" link directly and confirmed a visible 3px focus ring; no console errors.
- Full suite grew from 183 to 196 tests, all passing on a clean full run (`tests/components/{LibrarySummaryList,ClientMasterServiceAgreementSummary,ClientRateCardsSummary}.test.tsx` new; `RateCardsPanel.test.tsx` updated to drop the creation-flow assertions that moved elsewhere; `ChatPanel.test.tsx` flake fix); typecheck, lint, and production build all clean. Not yet pushed — committed locally pending user review, per this session's instruction.

### Bug fix: MSA upload appeared to succeed but silently never wrote (2026-08-26)

- **User report**: on the deployed `/master-service-agreements` library page, uploading a new MSA version showed no error and the upload form closed as if it worked, but the new version never appeared in the list — even after a refresh.
- **Root cause, two compounding bugs, both in shared code used by all three commercial document types (MSA/Rate Card/SOW Template), not MSA-specific**:
  1. **`VersionHistory.tsx`'s upload form always closed itself after submission, regardless of outcome.** The form's `action` was a wrapper (`async (formData) => { await formAction(formData); setIsUploading(false); }`) that called `setIsUploading(false)` unconditionally — including when the Server Action returned an error `{message}`. Since the error paragraph only renders while the form is open, any graceful failure (permission check, validation, unreadable file) was hidden in the same tick it would have appeared, making every failure look identical to success.
  2. **`extractTextFromBuffer.ts` swallowed any parse error into a generic, unlogged message.** Its catch block converted *any* exception from `officeparser`/`pdfjs-dist` (not just the expected unsupported-format case) into `{ message: "Couldn't read that file." }` with no `console.error` — so a real-world file (scanned/signed contract, embedded fonts, etc.) failing to parse in production would leave zero trace in Vercel's logs, on top of being invisible in the UI per bug 1. Confirmed the DB/transaction path itself is sound (parsing a well-formed synthetic PDF locally worked end-to-end, and Coffee's existing MSA row proves the same write path has succeeded before) — the missing document is consistent with the action returning early on a parse failure before the `$transaction` ever runs.
  3. **Also raised, as a live but unconfirmed-as-primary contributor**: Next.js Server Actions default to a 1MB request body limit; `next.config.ts` had no override, and a real signed/scanned MSA PDF can easily exceed that.
- **Fix**: 
  - `src/components/features/VersionHistory.tsx` — replaced the unconditional-close wrapper with a `useRef`-tracked pending→settled transition (`useEffect` on `[pending, state]`) that only closes the form when a submission actually finishes *without* an error message; the form's `action` is now `formAction` directly.
  - `src/lib/extractTextFromBuffer.ts` — added `console.error` logging the real caught error before returning the generic user-facing message, so future parse failures are visible in server logs.
  - `next.config.ts` — added `experimental.serverActions.bodySizeLimit: "10mb"`.
- **New regression tests** in `tests/components/VersionHistory.test.tsx` (9 → 11): submitting with a failing action keeps the form open and shows the error; submitting with a successful action closes it. Caught one test-authoring mistake in the process — `container.querySelector("form")` grabbed the *wrong* form (an older version's collapsed Revert form, still mounted in the DOM per `<details>`'s known stay-mounted-while-closed behavior, noted previously in this file) instead of the upload form; fixed by scoping via `screen.getByLabelText(...).closest("form")`.
- Verified: full suite green (196 → 198 tests), typecheck/lint/production build all clean, and a live dev-server check against the real Neon DB confirmed the MSA library page still renders existing data correctly post-fix (Coffee's real MSA unaffected). Could not drive an actual file-picker upload through either available browser automation surface in this session (neither supports native file dialogs against this dev server), so the fix is verified via the new component-level regression tests (which exercise the exact client-side state-transition bug directly) plus a direct local test of the parser against a synthetic PDF, rather than a full manual browser E2E of the upload button itself — worth a real manual click-through before/shortly after next deploying.
- **Deployed (commit `009fd90`) and the user retried a real upload — bug 1's fix worked (the real error, "Couldn't read that file.", is now surfaced) but the underlying failure was still there.** Queried the production Neon DB directly first (read-only): confirmed zero trace of the attempted upload — no version row, no `MasterServiceAgreement` document shell for Fizzy at all, meaning the write never started. Then pulled real production runtime logs via `vercel logs` (now populated thanks to the new logging) and found the true error, previously invisible: `TypeError: Cannot read properties of undefined (reading 'parseOffice')` — i.e. `OfficeParser` itself, not its output, was `undefined` at runtime in the deployed serverless function.
- **Real root cause: `officeparser` was being bundled by Turbopack into the Server Components chunk instead of loaded natively, and something about that bundling broke its named export.** Confirmed this wasn't a document-content issue — a hand-built, well-formed `.docx` (via `fflate`) parsed and extracted text correctly through the raw npm package locally (both `next dev` and a plain Node script), but neither of those code paths goes through Turbopack's actual *production* Server Components bundling step, so neither could have caught this. `officeparser` isn't on Next's built-in auto-externalized package list (`node_modules/next/dist/lib/server-external-packages.jsonc` equivalent, documented in `serverExternalPackages.md`) — a documented, known failure class for packages with dynamic `require()`/WASM/native dependencies (this one drags in `tesseract.js` for OCR and `pdfjs-dist` for PDF, both flagged in this exact category).
- **Fix**: added `serverExternalPackages: ["officeparser"]` to `next.config.ts`, telling Next to `require()` the package natively at runtime instead of bundling it. Verified concretely, not just by theory, before redeploying: after a fresh local `npm run build`, `.next/server/app/(dashboard)/master-service-agreements/page.js.nft.json`'s file-trace manifest went from bundling `officeparser` into the chunk to listing 58 real `node_modules/officeparser/**` files (including the actual working Node CJS entry, `dist/index.js`) as files to be copied into the serverless function — direct build-artifact evidence the externalization took effect, not just that the documented fix "should" apply here. Full test suite (198), typecheck, lint, and build all clean after the change (config-only, no app logic touched).
- **This bug is very unlikely to have been MSA-specific** — `extractTextFromBuffer`/`parseDocumentToText` is the same shared code path used by the Intake Agent's brief upload (task 4.0) and the Knowledge Upload feature (task 8.0) for every non-`.txt` file. Every "verified end-to-end in the browser" note earlier in this file was run against a local dev server, never the actual deployed Vercel build — so this class of bug had no opportunity to surface before now. Worth a real docx/pdf/pptx upload smoke test against production for the Intake Agent and Knowledge Upload flows too, once this deploys, not just MSA/Rate Card/SOW Template.
- **User confirmed the MSA fix worked, then asked to roll the fix out to every upload path (specifically calling out Rate Cards and SOW Templates).** `serverExternalPackages` is a build-wide config, not per-route — it already covered every upload (MSA/Rate Card/SOW Template/Knowledge Upload/Intake brief) the moment it deployed, no route-specific work needed there. But swept the codebase for the *other* bug (the client-side "form silently closes on any submit result" pattern originally found and fixed in `VersionHistory.tsx`) rather than assuming it was MSA-only, since it's a UI-layer bug independent of the parsing fix. Grepped every `type="file"` form in the app (5 total) and found the exact same anti-pattern in two more places — both are separate "quick-add creation" forms, not `VersionHistory` itself, so they weren't touched by the earlier MSA-focused fix: `ClientRateCardsSummary.tsx`'s "Add rate card" form and `ClientSowTemplatesSection.tsx`'s "Add [Client]-specific variant" form. The other two file-upload forms in the app (`KnowledgeUpload.tsx`, `NewProjectForm.tsx`) use `action={formAction}` directly with no collapsible-panel state to hide behind, so they were never affected.
- **Fix**: applied the identical `useRef`-tracked pending→settled-transition pattern from `VersionHistory.tsx` to both `ClientRateCardsSummary.tsx` and `ClientSowTemplatesSection.tsx` — the form's `action` is now `formAction` directly, and the "add" panel only auto-closes once a submission finishes *without* an error message.
- **New regression tests** (same shape as `VersionHistory.test.tsx`'s): `tests/components/ClientRateCardsSummary.test.tsx` and `tests/components/ClientSowTemplatesSection.test.tsx` each gained a "keeps the form open and shows the error on failure" + "closes on success" pair (9 → 15 combined). Caught one type error the full `npm run build` surfaced that plain `vitest run` did not (vitest's transform doesn't strictly typecheck) — the existing `vi.fn(async () => undefined)` mocks were inferred as `Promise<undefined>`-only, so `mockImplementationOnce(async () => ({ message: ... }))` didn't typecheck; fixed by giving both mocks an explicit `Promise<{ message?: string } | undefined>` return type. A reminder that `npm run build` (which runs `tsc`) is a stricter gate than the test runner alone.
- Full suite grew from 198 to 202, typecheck/lint/build all clean. Verified in a live local dev-server check post-fix: Fizzy's Client page renders the Rate Cards and SOW Templates "Add" controls correctly with no console errors (beyond the known-benign pg SSL warning). Could not drive a real file-picker upload through browser automation in this session (same limitation as before), so end-to-end confirmation of an actual successful upload still rests on the user's own retry once deployed.
